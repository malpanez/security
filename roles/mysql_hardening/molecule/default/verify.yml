---
- name: Verify mysql_hardening role
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: MySQL hardening verification complete
      ansible.builtin.debug:
        msg: "✓ MySQL hardening molecule verification complete (review mode — service may not be installed)"
