---
- name: Converge tomcat_hardening
  hosts: all
  become: true
  gather_facts: true

  vars:
    security_mode: review   # Safe in CI — service likely not installed

  roles:
    - role: malpanez.security.tomcat_hardening
