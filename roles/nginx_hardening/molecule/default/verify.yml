---
- name: Verify nginx_hardening role
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Nginx hardening verification complete
      ansible.builtin.debug:
        msg: "✓ Nginx hardening molecule verification complete (review mode — service may not be installed)"
