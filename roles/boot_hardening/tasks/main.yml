---
- name: Gather minimum facts if not already collected
  ansible.builtin.setup:
    gather_subset: [min]
  when: ansible_facts == {}
  tags: [always]

- name: Assert supported OS family
  ansible.builtin.assert:
    that:
      - ansible_os_family in ['Debian', 'RedHat', 'Suse']
    fail_msg: >-
      boot_hardening: unsupported OS family '{{ ansible_os_family }}'.
      Supported: Debian (update-grub), RedHat/Suse (grub2-mkconfig).
    quiet: true
  tags: [always]

- name: Load OS-specific variables
  ansible.builtin.include_vars: "{{ ansible_os_family }}.yml"
  tags: [always]

- name: Detect container environment
  ansible.builtin.set_fact:
    _boot_hardening_in_container: >-
      {{ ansible_facts.virtualization_type | default('') in
         ['docker', 'podman', 'container', 'lxc'] }}
  tags: [always]

- name: Review bootloader configuration
  ansible.builtin.include_tasks: review.yml
  tags: [boot_hardening, review]

- name: Enforce bootloader hardening
  ansible.builtin.include_tasks: enforce.yml
  when:
    - boot_hardening_enabled | bool
    - security_mode | default('review') == 'enforce'
  tags: [boot_hardening, enforce]
