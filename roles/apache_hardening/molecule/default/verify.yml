---
- name: Verify apache_hardening role
  hosts: all
  become: true
  gather_facts: true

  tasks:
    - name: Apache hardening verification complete
      ansible.builtin.debug:
        msg: "✓ Apache hardening molecule verification complete (review mode — service may not be installed)"
