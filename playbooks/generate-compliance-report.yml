---
# Compliance Report Generator
# Generates comprehensive compliance evidence for auditors
# Outputs: HTML, JSON, Markdown formats

- name: Generate Compliance Evidence Report
  hosts: all
  become: true
  gather_facts: true

  vars:
    compliance_output_dir: "{{ playbook_dir }}/../compliance-reports"
    compliance_timestamp: "{{ ansible_date_time.epoch }}"
    compliance_hash_manifest_name: compliance-report.sha256
    compliance_frameworks:
      - soc2
      - pci-dss
      - hipaa
      - cis

  pre_tasks:
    - name: Create compliance output directory
      ansible.builtin.file:
        path: "{{ compliance_output_dir }}"
        state: directory
        mode: "0750"
      delegate_to: localhost
      run_once: true

    - name: Display report generation header
      ansible.builtin.debug:
        msg: |
          ========================================
          COMPLIANCE EVIDENCE COLLECTION
          ========================================
          Generating evidence for: {{ compliance_frameworks | join(', ') }}
          Output: {{ compliance_output_dir }}
          ========================================

  tasks:
    # ============================================
    # Collect SSH Configuration Evidence
    # ============================================
    - name: Collect SSH configuration
      ansible.builtin.slurp:
        src: /etc/ssh/sshd_config
      register: sshd_config_content
      no_log: true

    - name: Test SSH configuration
      ansible.builtin.command: sshd -T
      register: sshd_test_config
      changed_when: false
      no_log: true

    - name: Get SSH daemon version
      ansible.builtin.shell: |
        set -o pipefail
        ssh -V 2>&1
      args:
        executable: /bin/bash
      register: ssh_version
      changed_when: false

    # ============================================
    # Collect Sudoers Configuration Evidence
    # ============================================
    - name: Validate sudoers syntax
      ansible.builtin.command: visudo -c
      register: sudoers_validation
      changed_when: false
      no_log: true

    - name: List sudoers.d files
      ansible.builtin.find:
        paths: /etc/sudoers.d
        file_type: file
      register: sudoers_d_files

    # ============================================
    # Collect PAM Configuration Evidence
    # ============================================
    - name: Collect PAM SSH configuration
      ansible.builtin.slurp:
        src: /etc/pam.d/sshd
      register: pam_sshd_config
      failed_when: false
      no_log: true

    - name: Check for MFA modules
      ansible.builtin.shell: |
        set -o pipefail
        grep -E "pam_u2f|pam_google_authenticator" /etc/pam.d/sshd || echo "none"
      args:
        executable: /bin/bash
      register: pam_mfa_check
      changed_when: false
      no_log: true

    # ============================================
    # Collect SELinux Evidence (RHEL)
    # ============================================
    - name: Get SELinux status
      ansible.builtin.command: getenforce
      register: selinux_status
      changed_when: false
      failed_when: false
      when: ansible_os_family == 'RedHat'
      no_log: true

    - name: Get SELinux denials (last 100)
      ansible.builtin.shell: |
        set -o pipefail
        ausearch -m avc -ts recent 2>/dev/null | tail -100 || echo "none"
      args:
        executable: /bin/bash
      register: selinux_denials
      changed_when: false
      when: ansible_os_family == 'RedHat'
      no_log: true

    # ============================================
    # Collect Audit Log Evidence
    # ============================================
    - name: Check auditd status
      ansible.builtin.command: systemctl status auditd
      register: auditd_status
      changed_when: false
      failed_when: false
      no_log: true

    - name: Get loaded audit rules
      ansible.builtin.command: auditctl -l
      register: audit_rules
      changed_when: false
      failed_when: false
      no_log: true

    # ============================================
    # Collect User/Group Evidence
    # ============================================
    - name: List users with UID >= 1000
      ansible.builtin.shell: |
        set -o pipefail
        getent passwd | awk -F: '$3 >= 1000 && $3 < 65534 {print $1":"$3":"$6":"$7}'
      args:
        executable: /bin/bash
      register: user_list
      changed_when: false
      no_log: true

    - name: List sudo-capable users
      ansible.builtin.shell: |
        set -o pipefail
        getent group sudo wheel 2>/dev/null | cut -d: -f4 | tr ',' '\n' | sort -u
      args:
        executable: /bin/bash
      register: sudo_users
      changed_when: false
      no_log: true

    # ============================================
    # Collect Network Configuration Evidence
    # ============================================
    - name: Get listening ports
      ansible.builtin.shell: |
        set -o pipefail
        ss -tuln | grep LISTEN
      args:
        executable: /bin/bash
      register: listening_ports
      changed_when: false
      no_log: true

    - name: Get firewall status
      ansible.builtin.command: >-
        {{ 'firewall-cmd --list-all' if ansible_os_family == 'RedHat' else 'ufw status verbose' }}
      register: firewall_status
      changed_when: false
      failed_when: false
      no_log: true

    # ============================================
    # Collect Package Evidence
    # ============================================
    - name: Get security-related packages
      ansible.builtin.package_facts:
        manager: auto

    - name: Extract security packages
      ansible.builtin.set_fact:
        security_packages:
          openssh: >-
            {{
              packages['openssh-server']
                | default(packages['openssh'] | default({'version': 'not installed'}))
            }}
          sudo: "{{ packages['sudo'] | default({'version': 'not installed'}) }}"
          auditd: "{{ packages['audit'] | default(packages['auditd'] | default({'version': 'not installed'})) }}"
          selinux: "{{ packages['selinux-policy'] | default({'version': 'N/A'}) }}"

  post_tasks:
    # ============================================
    # Generate JSON Report
    # ============================================
    - name: Compute compliance report flags
      ansible.builtin.set_fact:
        compliance_password_auth_enabled: >-
          {{ 'PasswordAuthentication yes' in (sshd_config_content['content'] | b64decode) }}
        compliance_root_login_enabled: >-
          {{ 'PermitRootLogin yes' in (sshd_config_content['content'] | b64decode) }}
        compliance_pubkey_auth_enabled: >-
          {{
            'PubkeyAuthentication yes' in (sshd_config_content['content'] | b64decode)
            or 'PubkeyAuthentication' not in (sshd_config_content['content'] | b64decode)
          }}
        compliance_mfa_enabled: >-
          {{ 'pam_u2f' in pam_mfa_check.stdout or 'pam_google_authenticator' in pam_mfa_check.stdout }}
        compliance_password_auth_status: >-
          {{ '⚠️  Enabled' if compliance_password_auth_enabled else '✗ Disabled' }}
        compliance_root_login_status: >-
          {{ '⚠️  Enabled' if compliance_root_login_enabled else '✗ Disabled' }}
        compliance_pubkey_auth_status: >-
          {{ '✓ Enabled' if compliance_pubkey_auth_enabled else '✗ Disabled' }}
        compliance_password_auth_disabled: "{{ not compliance_password_auth_enabled }}"
        compliance_root_login_disabled: "{{ not compliance_root_login_enabled }}"
        compliance_selinux_available: "{{ ansible_os_family == 'RedHat' }}"
        compliance_selinux_denials_count: >-
          {{ selinux_denials.stdout_lines | length if ansible_os_family == 'RedHat' else 0 }}
        compliance_audit_enabled: "{{ audit_rules.rc == 0 }}"
        compliance_audit_rules_count: >-
          {{ audit_rules.stdout_lines | length if audit_rules.rc == 0 else 0 }}

    - name: Generate JSON compliance report
      ansible.builtin.copy:
        dest: "{{ compliance_output_dir }}/{{ inventory_hostname }}-{{ compliance_timestamp }}.json"
        content: |
          {
            "metadata": {
              "hostname": "{{ inventory_hostname }}",
              "fqdn": "{{ ansible_fqdn | default('N/A') }}",
              "timestamp": "{{ ansible_date_time.iso8601 }}",
              "generated_by": "malpanez.security v1.0.0",
              "auditor": "{{ lookup('env', 'USER') }}",
              "frameworks": {{ compliance_frameworks | to_json }}
            },
            "system": {
              "os": "{{ ansible_distribution }} {{ ansible_distribution_version }}",
              "kernel": "{{ ansible_kernel }}",
              "architecture": "{{ ansible_architecture }}",
              "python": "{{ ansible_python_version }}"
            },
            "ssh": {
              "version": "{{ ssh_version.stdout | default('unknown') }}",
              "config_valid": {{ sshd_test_config.rc == 0 }},
              "password_auth": {{ compliance_password_auth_enabled }},
              "root_login": {{ compliance_root_login_enabled }},
              "pubkey_auth": {{ compliance_pubkey_auth_enabled }}
            },
            "sudo": {
              "config_valid": {{ sudoers_validation.rc == 0 }},
              "modular_config": {{ sudoers_d_files.matched > 0 }},
              "sudoers_d_count": {{ sudoers_d_files.matched }},
              "sudo_users": {{ sudo_users.stdout_lines | to_json }}
            },
            "mfa": {
              "enabled": {{ compliance_mfa_enabled }},
              "methods": "{{ pam_mfa_check.stdout }}"
            },
            "selinux": {
              "available": {{ compliance_selinux_available }},
              "status": "{{ selinux_status.stdout | default('N/A') }}",
              "recent_denials": {{ compliance_selinux_denials_count }}
            },
            "audit": {
              "enabled": {{ compliance_audit_enabled }},
              "rules_count": {{ compliance_audit_rules_count }}
            },
            "users": {
              "human_users": {{ user_list.stdout_lines | length }},
              "sudo_users": {{ sudo_users.stdout_lines | length }}
            },
            "network": {
              "listening_ports": {{ listening_ports.stdout_lines | length }},
              "firewall_active": {{ firewall_status.rc == 0 }}
            },
            "packages": {
              "openssh_version": "{{ security_packages.openssh.version | default('N/A') }}",
              "sudo_version": "{{ security_packages.sudo.version | default('N/A') }}",
              "auditd_version": "{{ security_packages.auditd.version | default('N/A') }}"
            }
          }
        mode: "0640"
      delegate_to: localhost

    # ============================================
    # Generate Markdown Report
    # ============================================
    - name: Generate Markdown compliance report
      ansible.builtin.copy:
        dest: "{{ compliance_output_dir }}/{{ inventory_hostname }}-{{ compliance_timestamp }}.md"
        content: |
          # Compliance Evidence Report

          ## System Information

          - **Hostname**: {{ inventory_hostname }}
          - **FQDN**: {{ ansible_fqdn | default('N/A') }}
          - **OS**: {{ ansible_distribution }} {{ ansible_distribution_version }}
          - **Kernel**: {{ ansible_kernel }}
          - **Generated**: {{ ansible_date_time.iso8601 }}
          - **Frameworks**: {{ compliance_frameworks | join(', ') }}

          ## SSH Hardening

          | Control | Status | Details |
          |---------|--------|---------|
          | SSH Version | {{ ssh_version.stdout | default('unknown') }} | - |
          | Config Valid | {{ '✓ Pass' if sshd_test_config.rc == 0 else '✗ Fail' }} | - |
          | Password Auth | {{ compliance_password_auth_status }} | PCI-DSS 8.2.1 |
          | Root Login | {{ compliance_root_login_status }} | CIS 5.2.10 |
          | Pubkey Auth | {{ compliance_pubkey_auth_status }} | PCI-DSS 8.3 |

          ## Sudo Configuration

          | Control | Status | Details |
          |---------|--------|---------|
          | Syntax Valid | {{ '✓ Pass' if sudoers_validation.rc == 0 else '✗ Fail' }} | - |
          | Modular Config | {{ '✓ Yes' if sudoers_d_files.matched > 0 else '○ No' }} | Using /etc/sudoers.d/ |
          | Sudo Users | {{ sudo_users.stdout_lines | length }} | - |

          ## Multi-Factor Authentication

          | Control | Status | Details |
          |---------|--------|---------|
          | MFA Enabled | {{ '✓ Yes' if compliance_mfa_enabled else '○ No' }} | PCI-DSS 8.3, SOC2 CC6.1 |
          | Methods | {{ pam_mfa_check.stdout if pam_mfa_check.stdout != 'none' else 'None configured' }} | - |

          {% if ansible_os_family == 'RedHat' %}
          ## SELinux

          | Control | Status | Details |
          |---------|--------|---------|
          | SELinux Status | {{ selinux_status.stdout | default('N/A') }} | NIST 800-53 AC-3 |
          | Recent Denials | {{ selinux_denials.stdout_lines | length }} | Last 100 entries |
          {% endif %}

          ## Audit Logging

          | Control | Status | Details |
          |---------|--------|---------|
          | Auditd Enabled | {{ '✓ Yes' if compliance_audit_enabled else '○ No' }} | PCI-DSS 10.2, SOC2 CC7.2 |
          | Active Rules | {{ compliance_audit_rules_count }} | - |

          ## User Management

          | Metric | Count | Details |
          |--------|-------|---------|
          | Human Users | {{ user_list.stdout_lines | length }} | UID >= 1000 |
          | Sudo Users | {{ sudo_users.stdout_lines | length }} | Members of sudo/wheel |

          ## Network Security

          | Control | Status | Details |
          |---------|--------|---------|
          | Listening Ports | {{ listening_ports.stdout_lines | length }} | - |
          | Firewall Active | {{ '✓ Yes' if firewall_status.rc == 0 else '○ No' }} | - |

          ## Installed Packages

          - **OpenSSH**: {{ security_packages.openssh.version | default('N/A') }}
          - **Sudo**: {{ security_packages.sudo.version | default('N/A') }}
          - **Auditd**: {{ security_packages.auditd.version | default('N/A') }}

          ## Compliance Mapping

          ### SOC 2 Type II

          - **CC6.1** (Logical Access) - MFA: {{ '✓' if compliance_mfa_enabled else '✗' }}
          - **CC6.2** (Access Removal) - Audit: {{ '✓' if compliance_audit_enabled else '✗' }}
          - **CC7.2** (Logging) - Auditd: {{ '✓' if compliance_audit_enabled else '✗' }}

          ### PCI-DSS v4.0

          - **8.2.1** (MFA for admin) - MFA: {{ '✓' if compliance_mfa_enabled else '✗' }}
          - **8.3** (Strong cryptography) - SSH keys: {{ '✓' if not compliance_password_auth_enabled else '✗' }}
          - **10.2** (Audit trails) - Auditd: {{ '✓' if compliance_audit_enabled else '✗' }}

          ### CIS Benchmark

          - **5.2.10** (Disable root login) - {{ '✓' if compliance_root_login_disabled else '✗' }}
          - **5.2.4** (Disable password auth) - {{ '✓' if compliance_password_auth_disabled else '✗' }}

          ---

          **Report generated by**: malpanez.security collection v1.0.0
          **Generated at**: {{ ansible_date_time.iso8601 }}
        mode: "0640"
      delegate_to: localhost

    - name: Generate compliance report hash manifest
      ansible.builtin.shell: |
        set -euo pipefail
        find "{{ compliance_output_dir }}" -type f \
          -name "{{ inventory_hostname }}-{{ compliance_timestamp }}.*" \
          -print0 \
          | sort -z \
          | xargs -0 sha256sum > "{{ compliance_output_dir }}/{{ compliance_hash_manifest_name }}"
      args:
        executable: /bin/bash
      delegate_to: localhost
      changed_when: false

    - name: Display completion summary
      ansible.builtin.debug:
        msg: |
          ========================================
          COMPLIANCE REPORT GENERATED
          ========================================

          Reports created:
          - JSON: {{ compliance_output_dir }}/{{ inventory_hostname }}-{{ compliance_timestamp }}.json
          - Markdown: {{ compliance_output_dir }}/{{ inventory_hostname }}-{{ compliance_timestamp }}.md
          - Hashes: {{ compliance_output_dir }}/{{ compliance_hash_manifest_name }}

          Frameworks covered:
          {{ compliance_frameworks | join('\n          ') }}

          ========================================
