---
phase: 06-usbguard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/usbguard/defaults/main.yml
  - roles/usbguard/vars/RedHat.yml
  - roles/usbguard/vars/Debian.yml
  - roles/usbguard/vars/Suse.yml
  - roles/usbguard/meta/main.yml
  - roles/usbguard/meta/argument_specs.yml
  - roles/usbguard/handlers/main.yml
  - roles/usbguard/tests/test.yml
  - roles/usbguard/README.md
autonomous: true
requirements:
  - STD-01
  - STD-05
  - STD-06
  - STD-07
  - STD-08
  - STD-09
  - USB-07
must_haves:
  truths:
    - "usbguard role directory exists with ansible-galaxy role init structure"
    - "defaults/main.yml declares all configurable variables with safe defaults"
    - "OS vars files provide correct package names, paths, and IPC groups per OS family"
    - "meta/main.yml passes Galaxy schema validation"
    - "argument_specs.yml documents every default variable with type and description"
  artifacts:
    - path: "roles/usbguard/defaults/main.yml"
      provides: "All usbguard default variables"
      contains: "usbguard_enabled"
    - path: "roles/usbguard/vars/RedHat.yml"
      provides: "RedHat-specific vars including EPEL flag and wheel group"
      contains: "_usbguard_ipc_group: wheel"
    - path: "roles/usbguard/vars/Debian.yml"
      provides: "Debian-specific vars with sudo group"
      contains: "_usbguard_ipc_group: sudo"
    - path: "roles/usbguard/vars/Suse.yml"
      provides: "Suse-specific vars with wheel group"
      contains: "_usbguard_ipc_group: wheel"
    - path: "roles/usbguard/meta/main.yml"
      provides: "Galaxy metadata with compliance tags"
      contains: "pcidss"
    - path: "roles/usbguard/meta/argument_specs.yml"
      provides: "Full argument documentation"
      contains: "usbguard_implicit_policy"
    - path: "roles/usbguard/handlers/main.yml"
      provides: "Restart usbguard handler"
      contains: "Restart usbguard"
    - path: "roles/usbguard/tests/test.yml"
      provides: "FQCN role reference test"
      contains: "malpanez.security.usbguard"
  key_links:
    - from: "roles/usbguard/defaults/main.yml"
      to: "roles/usbguard/meta/argument_specs.yml"
      via: "every default var documented in argument_specs"
      pattern: "usbguard_"
---

<objective>
Scaffold the usbguard role using ansible-galaxy role init, then populate defaults, OS vars, meta, argument_specs, handlers, and tests.

Purpose: Establish the role skeleton with all variable contracts before tasks/templates are written in subsequent plans.
Output: Complete role scaffold with defaults, vars, meta, handlers, tests — ready for tasks implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-usbguard/06-RESEARCH.md

@roles/ntp_hardening/defaults/main.yml
@roles/ntp_hardening/meta/main.yml
@roles/ntp_hardening/meta/argument_specs.yml
@roles/ntp_hardening/handlers/main.yml
@roles/ntp_hardening/tests/test.yml
@roles/ntp_hardening/vars/RedHat.yml

<interfaces>
<!-- Reference patterns from ntp_hardening role (same structure applies) -->

From roles/ntp_hardening/defaults/main.yml:
```yaml
ntp_hardening_enabled: false
```

From roles/ntp_hardening/meta/main.yml:
```yaml
galaxy_info:
  platforms:
    - name: EL
      versions: [all]
```

From roles/ntp_hardening/handlers/main.yml:
```yaml
- name: Restart chronyd
  ansible.builtin.systemd:
    name: "{{ ntp_hardening_service }}"
    state: restarted
  changed_when: true
  when: not _ntp_in_container | bool
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize role scaffold and populate defaults + OS vars</name>
  <files>
    roles/usbguard/defaults/main.yml
    roles/usbguard/vars/RedHat.yml
    roles/usbguard/vars/Debian.yml
    roles/usbguard/vars/Suse.yml
  </files>
  <read_first>
    roles/ntp_hardening/defaults/main.yml
    roles/ntp_hardening/vars/RedHat.yml
    .planning/phases/06-usbguard/06-RESEARCH.md (vars sections)
  </read_first>
  <action>
Run `ansible-galaxy role init usbguard --init-path=roles/` to create the role skeleton (STD-01).

Then populate `defaults/main.yml` with these exact variables and values:
- `usbguard_enabled: false`
- `usbguard_implicit_policy: block`
- `usbguard_allow_existing_devices: true`
- `usbguard_present_device_policy: apply-policy`
- `usbguard_present_controller_policy: apply-policy`
- `usbguard_restore_controller_state: false`
- `usbguard_audit_backend: LinuxAudit`
- `usbguard_audit_file_path: /var/log/usbguard/usbguard-audit.log`
- `usbguard_extra_rules: []`

Populate `vars/RedHat.yml`:
- `_usbguard_package: usbguard`
- `_usbguard_service: usbguard`
- `_usbguard_conf_dir: /etc/usbguard`
- `_usbguard_rules_dir: /etc/usbguard/rules.d`
- `_usbguard_daemon_conf: /etc/usbguard/usbguard-daemon.conf`
- `_usbguard_initial_rules: /etc/usbguard/rules.d/00-initial-policy.conf`
- `_usbguard_needs_epel: "{{ ansible_distribution_major_version | int == 8 }}"`
- `_usbguard_ipc_group: wheel`

Populate `vars/Debian.yml`:
- Same path variables as RedHat (all under /etc/usbguard/)
- `_usbguard_needs_epel: false`
- `_usbguard_ipc_group: sudo` (Debian uses sudo group, not wheel)

Populate `vars/Suse.yml`:
- Same path variables as RedHat
- `_usbguard_needs_epel: false`
- `_usbguard_ipc_group: wheel`

All YAML files: 2-space indent, no comments (per project conventions). FQCN not applicable in vars files (STD-05 applies to tasks).
  </action>
  <verify>
    <automated>yamllint roles/usbguard/defaults/main.yml roles/usbguard/vars/RedHat.yml roles/usbguard/vars/Debian.yml roles/usbguard/vars/Suse.yml</automated>
  </verify>
  <acceptance_criteria>
    - `roles/usbguard/defaults/main.yml` contains exactly 9 variables starting with `usbguard_`
    - `grep 'usbguard_enabled: false' roles/usbguard/defaults/main.yml` succeeds
    - `grep 'usbguard_implicit_policy: block' roles/usbguard/defaults/main.yml` succeeds
    - `grep '_usbguard_ipc_group: wheel' roles/usbguard/vars/RedHat.yml` succeeds
    - `grep '_usbguard_ipc_group: sudo' roles/usbguard/vars/Debian.yml` succeeds
    - `grep '_usbguard_ipc_group: wheel' roles/usbguard/vars/Suse.yml` succeeds
    - `grep '_usbguard_needs_epel' roles/usbguard/vars/RedHat.yml` shows Jinja2 expression with `ansible_distribution_major_version | int == 8`
    - `grep '_usbguard_needs_epel: false' roles/usbguard/vars/Debian.yml` succeeds
    - yamllint exits 0 on all 4 files
  </acceptance_criteria>
  <done>Role scaffold created via ansible-galaxy role init. defaults/main.yml has all 9 usbguard_ variables. All 3 OS vars files have correct package, service, path, EPEL, and IPC group values.</done>
</task>

<task type="auto">
  <name>Task 2: Populate meta, argument_specs, handlers, and tests</name>
  <files>
    roles/usbguard/meta/main.yml
    roles/usbguard/meta/argument_specs.yml
    roles/usbguard/handlers/main.yml
    roles/usbguard/tests/test.yml
  </files>
  <read_first>
    roles/ntp_hardening/meta/main.yml
    roles/ntp_hardening/meta/argument_specs.yml
    roles/ntp_hardening/handlers/main.yml
    roles/ntp_hardening/tests/test.yml
  </read_first>
  <action>
Populate `meta/main.yml` (STD-07, USB-07):
```yaml
galaxy_info:
  author: Miguel Alpanez
  description: >-
    USB device control with USBGuard. Installs usbguard, generates initial
    device whitelist from attached hardware, deploys block-by-default policy
    via usbguard-daemon.conf. Supports review mode (audit-only) and enforce
    mode. PCI-DSS 12.3.4, HIPAA 164.310(d)(1), NIS2 Art.21, SOC2 CC6.7.
  license: GPL-2.0-or-later
  min_ansible_version: "2.16"
  galaxy_tags:
    - security
    - hardening
    - usbguard
    - usb
    - devicecontrol
    - linux
    - pcidss
    - nis2
    - soc2
    - hipaa
    - compliance
  platforms:
    - name: Debian
      versions: [buster, bullseye, bookworm]
    - name: Ubuntu
      versions: [bionic, focal, jammy]
    - name: EL
      versions: [all]
dependencies: []
```

Note: No hyphens in galaxy_tags (use `pcidss` not `pci-dss` per project convention). No Suse platform entry (Galaxy schema rejects it, per tls_hardening decision).

Populate `meta/argument_specs.yml` (STD-08) — document ALL 9 defaults variables with type, default, description. For `usbguard_implicit_policy` add `choices: [allow, block, reject]`. For `usbguard_present_device_policy` add `choices: [allow, block, reject, apply-policy, keep]`. For `usbguard_present_controller_policy` same choices. For `usbguard_audit_backend` add `choices: [LinuxAudit, FileAudit]`.

Populate `handlers/main.yml`:
```yaml
---
- name: Restart usbguard
  ansible.builtin.service:
    name: "{{ _usbguard_service }}"
    state: restarted
  changed_when: true
  when: _usbguard_usb_available | default(false) | bool
```

Use `ansible.builtin.service` (not systemd) per ROADMAP spec. Guard with `_usbguard_usb_available` to prevent handler fire in containers.

Populate `tests/test.yml` (STD-09):
```yaml
---
- name: Test usbguard role
  hosts: localhost
  gather_facts: true
  roles:
    - role: malpanez.security.usbguard
```
  </action>
  <verify>
    <automated>yamllint roles/usbguard/meta/main.yml roles/usbguard/meta/argument_specs.yml roles/usbguard/handlers/main.yml roles/usbguard/tests/test.yml && ansible-lint --profile production roles/usbguard/meta/main.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'pcidss' roles/usbguard/meta/main.yml` succeeds (USB-07 compliance tag)
    - `grep 'nis2' roles/usbguard/meta/main.yml` succeeds
    - `grep 'hipaa' roles/usbguard/meta/main.yml` succeeds
    - `grep 'soc2' roles/usbguard/meta/main.yml` succeeds
    - `grep "versions: \[all\]" roles/usbguard/meta/main.yml` succeeds (EL versions format)
    - No `Suse` platform entry in meta/main.yml
    - `grep 'usbguard_implicit_policy' roles/usbguard/meta/argument_specs.yml` succeeds
    - `grep 'usbguard_extra_rules' roles/usbguard/meta/argument_specs.yml` succeeds
    - `grep 'Restart usbguard' roles/usbguard/handlers/main.yml` succeeds
    - `grep 'ansible.builtin.service' roles/usbguard/handlers/main.yml` succeeds
    - `grep '_usbguard_usb_available' roles/usbguard/handlers/main.yml` succeeds
    - `grep 'malpanez.security.usbguard' roles/usbguard/tests/test.yml` succeeds
    - yamllint exits 0 on all 4 files
    - ansible-lint exits 0 on meta/main.yml
  </acceptance_criteria>
  <done>meta/main.yml has valid Galaxy schema with compliance tags (pcidss, hipaa, nis2, soc2). argument_specs documents all 9 defaults with types and choices. Handler uses ansible.builtin.service with USB availability guard. tests/test.yml references FQCN.</done>
</task>

</tasks>

<verification>
- `yamllint roles/usbguard/` exits 0
- `ansible-lint --profile production roles/usbguard/meta/` exits 0
- All files under `roles/usbguard/` match ansible-galaxy role init structure
- `grep -r 'usbguard_' roles/usbguard/defaults/main.yml | wc -l` returns 9
</verification>

<success_criteria>
- Role skeleton exists at roles/usbguard/ with all standard directories
- defaults/main.yml has 9 variables with correct defaults
- 3 OS vars files have correct platform-specific values
- meta/main.yml passes Galaxy schema validation
- argument_specs.yml documents all defaults
- Handler guards against container execution
- tests/test.yml uses FQCN
- yamllint passes on all files
</success_criteria>

<output>
After completion, create `.planning/phases/06-usbguard/06-01-SUMMARY.md`
</output>
