---
phase: 06-usbguard
plan: 05
type: execute
wave: 4
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - .github/workflows/ci-uv.yml
  - .github/workflows/ci-cd-enterprise.yml
  - galaxy.yml
  - CHANGELOG.md
autonomous: true
requirements:
  - STD-12
  - STD-13
  - STD-14

must_haves:
  truths:
    - "usbguard appears in ci-uv.yml test-roles matrix"
    - "usbguard appears in ci-cd-enterprise.yml test-molecule matrix"
    - "yamllint and ansible-lint pass with 0 errors on entire role"
    - "galaxy.yml version is 1.2.0"
    - "CHANGELOG.md has [1.2.0] section listing all 6 new roles"
    - "ansible-galaxy collection build --force succeeds"
  artifacts:
    - path: ".github/workflows/ci-uv.yml"
      provides: "CI matrix with usbguard"
      contains: "usbguard"
    - path: ".github/workflows/ci-cd-enterprise.yml"
      provides: "Enterprise CI matrix with usbguard"
      contains: "usbguard"
    - path: "galaxy.yml"
      provides: "Collection version 1.2.0"
      contains: "version: 1.2.0"
    - path: "CHANGELOG.md"
      provides: "Release notes for v1.2.0"
      contains: "[1.2.0]"
  key_links:
    - from: ".github/workflows/ci-uv.yml"
      to: "roles/usbguard/"
      via: "matrix entry triggers molecule test"
      pattern: "usbguard"
    - from: "galaxy.yml"
      to: "CHANGELOG.md"
      via: "version bump matches changelog entry"
      pattern: "1.2.0"
---

<objective>
Add usbguard to CI matrices, run full lint suite, bump galaxy.yml to 1.2.0, update CHANGELOG.md, and build the collection tarball.

Purpose: Close the v1.2.0 milestone: CI integration ensures the role is tested in pipelines, version bump and changelog document the release.
Output: CI-integrated role, version 1.2.0, changelog updated, collection tarball builds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-usbguard/06-RESEARCH.md

@.github/workflows/ci-uv.yml
@.github/workflows/ci-cd-enterprise.yml
@galaxy.yml
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add usbguard to CI matrices and run full lint</name>
  <files>
    .github/workflows/ci-uv.yml
    .github/workflows/ci-cd-enterprise.yml
  </files>
  <read_first>
    .github/workflows/ci-uv.yml
    .github/workflows/ci-cd-enterprise.yml
  </read_first>
  <action>
**CI matrix updates (STD-13):**

In `.github/workflows/ci-uv.yml`, find the `test-roles` job `matrix.role` list. Add `- usbguard` after the existing `- tls_hardening` entry. This follows the ROADMAP phase sequence ordering (ntp_hardening, user_audit, rsyslog_forwarding, antivirus, tls_hardening, usbguard).

In `.github/workflows/ci-cd-enterprise.yml`, find the `test-molecule` job `matrix.role` list. Add `- usbguard` after `- tls_hardening`.

**Collection reinstall (STD-14):**
Run: `ansible-galaxy collection install . --force -p .ansible/collections`

**Full lint suite (STD-12):**
Run: `yamllint roles/usbguard/`
Run: `ansible-lint --profile production roles/usbguard/`

Both must exit with 0 errors. Fix any issues before proceeding.
  </action>
  <verify>
    <automated>grep 'usbguard' .github/workflows/ci-uv.yml && grep 'usbguard' .github/workflows/ci-cd-enterprise.yml && ansible-galaxy collection install . --force -p .ansible/collections 2>&1 | tail -1 && yamllint roles/usbguard/ && ansible-lint --profile production roles/usbguard/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'usbguard' .github/workflows/ci-uv.yml` returns at least 1
    - `grep -c 'usbguard' .github/workflows/ci-cd-enterprise.yml` returns at least 1
    - usbguard appears AFTER tls_hardening in both CI matrix lists
    - `ansible-galaxy collection install . --force -p .ansible/collections` exits 0
    - `yamllint roles/usbguard/` exits 0
    - `ansible-lint --profile production roles/usbguard/` exits 0
  </acceptance_criteria>
  <done>usbguard added to both CI matrices after tls_hardening. Collection reinstalled. yamllint and ansible-lint pass with 0 errors on entire role.</done>
</task>

<task type="auto">
  <name>Task 2: Bump galaxy.yml to 1.2.0, update CHANGELOG.md, build collection</name>
  <files>
    galaxy.yml
    CHANGELOG.md
  </files>
  <read_first>
    galaxy.yml
    CHANGELOG.md
    .planning/phases/06-usbguard/06-RESEARCH.md (CHANGELOG entry format section)
  </read_first>
  <action>
**Version bump:**
In `galaxy.yml`, change `version: 1.1.0` to `version: 1.2.0`.

**CHANGELOG update:**
In `CHANGELOG.md`, replace the `## [Unreleased]` section. Keep the existing Unreleased content but promote it under `## [1.2.0] - 2026-04-20`. Add the 6 new roles to the `### Added` section. The final structure should be:

```markdown
## [Unreleased]

## [1.2.0] - 2026-04-20

### Added
- ntp_hardening role: chrony hardening with review/enforce mode (PCI-DSS 10.6.1, NIS2, SOC2 CC6.1)
- user_audit role: local account audit and expiry enforcement (SOC2 CC6.2, HIPAA, PCI-DSS 8.x)
- rsyslog_forwarding role: centralised log forwarding with optional TLS (NIS2, SOC2 CC7.2, PCI-DSS 10.5)
- antivirus role: ClamAV with freshclam and scan timer (PCI-DSS 5.2, NIS2, SOC2 CC6.8)
- tls_hardening role: system-wide TLS policy enforcement (PCI-DSS 4.2.1, HIPAA, NIS2)
- usbguard role: USB block-by-default with device whitelist (PCI-DSS 12.3.4, HIPAA, NIS2, SOC2 CC6.7)
- Renovate Bot integration for automated dependency management
- Molecule CI scenario with optimized 3-platform testing
- community.docker collection for Molecule Docker driver support
- Complete stack testing infrastructure with 11 platform coverage

### Changed
- Updated all GitHub Actions workflows to v5/v6
- Optimized CI execution time from 90+ to ~15-20 minutes
- Migrated from Dependabot to Renovate for better GitFlow integration

### Fixed
- Added missing community.docker collection requirement
- Improved molecule-complete-stack workflow variable handling

## [0.1.0] - Initial Release
...
```

**Build collection:**
Run: `ansible-galaxy collection build --force`
Verify it exits 0 and produces a `.tar.gz` file.
  </action>
  <verify>
    <automated>grep 'version: 1.2.0' galaxy.yml && grep '\[1.2.0\]' CHANGELOG.md && grep 'usbguard' CHANGELOG.md && ansible-galaxy collection build --force 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'version: 1.2.0' galaxy.yml` succeeds (was 1.1.0)
    - `grep '\[1.2.0\] - 2026-04-20' CHANGELOG.md` succeeds
    - `grep 'usbguard role' CHANGELOG.md` succeeds
    - `grep 'ntp_hardening role' CHANGELOG.md` succeeds
    - `grep 'user_audit role' CHANGELOG.md` succeeds
    - `grep 'rsyslog_forwarding role' CHANGELOG.md` succeeds
    - `grep 'antivirus role' CHANGELOG.md` succeeds
    - `grep 'tls_hardening role' CHANGELOG.md` succeeds
    - All 6 new roles listed in the [1.2.0] Added section
    - `## [Unreleased]` section exists (empty, above [1.2.0])
    - `ansible-galaxy collection build --force` exits 0
    - A `malpanez-security-1.2.0.tar.gz` file exists
  </acceptance_criteria>
  <done>galaxy.yml bumped to 1.2.0. CHANGELOG.md has [1.2.0] section with all 6 roles and compliance tags. Collection tarball builds successfully. Milestone v1.2.0 closed.</done>
</task>

</tasks>

<verification>
- `grep 'version: 1.2.0' galaxy.yml` succeeds
- `grep '\[1.2.0\]' CHANGELOG.md` succeeds
- `grep 'usbguard' .github/workflows/ci-uv.yml .github/workflows/ci-cd-enterprise.yml` shows both files
- `ls malpanez-security-1.2.0.tar.gz` succeeds
- `yamllint roles/usbguard/` exits 0
- `ansible-lint --profile production roles/usbguard/` exits 0
</verification>

<success_criteria>
- usbguard in both CI matrices (STD-13)
- Collection reinstalled (STD-14)
- yamllint + ansible-lint pass with 0 errors (STD-12)
- galaxy.yml version: 1.2.0
- CHANGELOG.md [1.2.0] section lists all 6 new roles
- Collection tarball builds successfully
- v1.2.0 milestone closed
</success_criteria>

<output>
After completion, create `.planning/phases/06-usbguard/06-05-SUMMARY.md`
</output>
