---
phase: 03-rsyslog-forwarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/rsyslog_forwarding/defaults/main.yml
  - roles/rsyslog_forwarding/vars/Debian.yml
  - roles/rsyslog_forwarding/vars/RedHat.yml
  - roles/rsyslog_forwarding/vars/Suse.yml
  - roles/rsyslog_forwarding/meta/main.yml
  - roles/rsyslog_forwarding/meta/argument_specs.yml
  - roles/rsyslog_forwarding/handlers/main.yml
  - roles/rsyslog_forwarding/tests/test.yml
autonomous: true
requirements: [STD-01, STD-05, STD-06, STD-07, STD-08, STD-09, STD-14, LOG-05, LOG-06]
must_haves:
  truths:
    - "Role directory exists at roles/rsyslog_forwarding/ with Galaxy-standard structure"
    - "All defaults variables are documented in argument_specs.yml"
    - "OS-specific package names are correct per OS family (rsyslog-module-gtls on Suse)"
    - "Handler restarts rsyslog with container guard"
  artifacts:
    - path: "roles/rsyslog_forwarding/defaults/main.yml"
      provides: "All rsyslog_forwarding_* default variables"
      contains: "rsyslog_forwarding_enabled: false"
    - path: "roles/rsyslog_forwarding/vars/Debian.yml"
      provides: "Debian package/service names"
      contains: "rsyslog_forwarding_tls_package: rsyslog-gnutls"
    - path: "roles/rsyslog_forwarding/vars/RedHat.yml"
      provides: "RedHat package/service names"
      contains: "rsyslog_forwarding_tls_package: rsyslog-gnutls"
    - path: "roles/rsyslog_forwarding/vars/Suse.yml"
      provides: "Suse package/service names"
      contains: "rsyslog_forwarding_tls_package: rsyslog-module-gtls"
    - path: "roles/rsyslog_forwarding/meta/main.yml"
      provides: "Galaxy metadata with compliance tags"
      contains: "pci"
    - path: "roles/rsyslog_forwarding/meta/argument_specs.yml"
      provides: "Variable documentation for all defaults"
      contains: "rsyslog_forwarding_host"
    - path: "roles/rsyslog_forwarding/handlers/main.yml"
      provides: "Restart rsyslog handler"
      contains: "changed_when: true"
    - path: "roles/rsyslog_forwarding/tests/test.yml"
      provides: "FQCN test playbook"
      contains: "malpanez.security.rsyslog_forwarding"
  key_links:
    - from: "roles/rsyslog_forwarding/defaults/main.yml"
      to: "roles/rsyslog_forwarding/meta/argument_specs.yml"
      via: "1:1 variable documentation"
      pattern: "rsyslog_forwarding_"
---

<objective>
Scaffold the rsyslog_forwarding role using ansible-galaxy role init, then populate defaults/main.yml with all configuration variables, OS-family vars, Galaxy metadata with compliance tags, argument_specs, handler, and tests/test.yml.

Purpose: Establish the role skeleton and all variable contracts that tasks and templates will consume.
Output: Complete role scaffold ready for tasks (plan 03-02) and templates (plan 03-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rsyslog-forwarding/03-CONTEXT.md
@.planning/phases/03-rsyslog-forwarding/03-RESEARCH.md

<interfaces>
<!-- Reference implementations from ntp_hardening — executor must replicate these patterns exactly -->

From roles/ntp_hardening/defaults/main.yml:
```yaml
ntp_hardening_enabled: false
ntp_hardening_servers:
  - 0.pool.ntp.org iburst
```

From roles/ntp_hardening/vars/Debian.yml:
```yaml
ntp_hardening_package: chrony
ntp_hardening_service: chrony
ntp_hardening_config_file: /etc/chrony/chrony.conf
ntp_hardening_conf_d: /etc/chrony/conf.d
```

From roles/ntp_hardening/vars/RedHat.yml:
```yaml
ntp_hardening_package: chrony
ntp_hardening_service: chronyd
ntp_hardening_config_file: /etc/chrony.conf
ntp_hardening_conf_d: /etc/chrony.d
```

From roles/ntp_hardening/meta/main.yml:
```yaml
galaxy_info:
  author: Miguel Alpanez
  description: >-
    NTP hardening with chrony. ...
  license: GPL-2.0-or-later
  min_ansible_version: "2.16"
  galaxy_tags: [security, hardening, ntp, chrony, ...]
  platforms:
    - name: Debian
      versions: [buster, bullseye, bookworm]
    - name: Ubuntu
      versions: [bionic, focal, jammy]
    - name: EL
      versions: [all]
dependencies: []
```

From roles/ntp_hardening/handlers/main.yml:
```yaml
- name: Restart chronyd
  ansible.builtin.systemd:
    name: "{{ ntp_hardening_service }}"
    state: restarted
  changed_when: true
  when: not _ntp_in_container | bool
```

From roles/ntp_hardening/tests/test.yml:
```yaml
- name: Test ntp_hardening role
  hosts: localhost
  gather_facts: true
  roles:
    - role: malpanez.security.ntp_hardening
```

From roles/ntp_hardening/meta/argument_specs.yml:
```yaml
argument_specs:
  main:
    short_description: NTP hardening with chrony
    options:
      ntp_hardening_enabled:
        type: bool
        default: false
        description: >-
          Gate variable. Set to true with security_mode=enforce to install and
          configure chrony. Review tasks always run regardless.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold role and populate defaults, vars, meta, handlers, tests</name>
  <files>
    roles/rsyslog_forwarding/defaults/main.yml,
    roles/rsyslog_forwarding/vars/Debian.yml,
    roles/rsyslog_forwarding/vars/RedHat.yml,
    roles/rsyslog_forwarding/vars/Suse.yml,
    roles/rsyslog_forwarding/meta/main.yml,
    roles/rsyslog_forwarding/meta/argument_specs.yml,
    roles/rsyslog_forwarding/handlers/main.yml,
    roles/rsyslog_forwarding/tests/test.yml
  </files>
  <read_first>
    roles/ntp_hardening/defaults/main.yml,
    roles/ntp_hardening/vars/Debian.yml,
    roles/ntp_hardening/vars/RedHat.yml,
    roles/ntp_hardening/meta/main.yml,
    roles/ntp_hardening/meta/argument_specs.yml,
    roles/ntp_hardening/handlers/main.yml,
    roles/ntp_hardening/tests/test.yml
  </read_first>
  <action>
1. Run `ansible-galaxy role init roles/rsyslog_forwarding --offline` to create the role skeleton (per STD-01).

2. Write `roles/rsyslog_forwarding/defaults/main.yml` with exactly these variables (per D-01 through D-09, LOG-05):
```yaml
rsyslog_forwarding_enabled: false

rsyslog_forwarding_host: ""
rsyslog_forwarding_port: 514
rsyslog_forwarding_protocol: tcp
rsyslog_forwarding_selector: "*.*"

rsyslog_forwarding_queue_type: LinkedList
rsyslog_forwarding_queue_size: 10000
rsyslog_forwarding_queue_spool_dir: /var/spool/rsyslog/forwarding
rsyslog_forwarding_queue_file_name: fwd-queue
rsyslog_forwarding_resume_retry_count: -1

rsyslog_forwarding_tls_enabled: false
rsyslog_forwarding_tls_ca_cert: ""
rsyslog_forwarding_tls_auth_mode: x509/name
rsyslog_forwarding_tls_client_cert: ""
rsyslog_forwarding_tls_client_key: ""

rsyslog_forwarding_logrotate_enabled: true
rsyslog_forwarding_logrotate_rotate: 4
rsyslog_forwarding_logrotate_size: 50M
rsyslog_forwarding_log_dir: /var/log
rsyslog_forwarding_file_create_mode: "0640"
```

3. Write `roles/rsyslog_forwarding/vars/Debian.yml` (per STD-06):
```yaml
rsyslog_forwarding_package: rsyslog
rsyslog_forwarding_tls_package: rsyslog-gnutls
rsyslog_forwarding_service: rsyslog
rsyslog_forwarding_dropin_dir: /etc/rsyslog.d
```

4. Write `roles/rsyslog_forwarding/vars/RedHat.yml`:
```yaml
rsyslog_forwarding_package: rsyslog
rsyslog_forwarding_tls_package: rsyslog-gnutls
rsyslog_forwarding_service: rsyslog
rsyslog_forwarding_dropin_dir: /etc/rsyslog.d
```

5. Write `roles/rsyslog_forwarding/vars/Suse.yml` (rsyslog-module-gtls per Suse pitfall):
```yaml
rsyslog_forwarding_package: rsyslog
rsyslog_forwarding_tls_package: rsyslog-module-gtls
rsyslog_forwarding_service: rsyslog
rsyslog_forwarding_dropin_dir: /etc/rsyslog.d
```

6. Write `roles/rsyslog_forwarding/meta/main.yml` following the ntp_hardening pattern exactly (per STD-07, LOG-06):
```yaml
galaxy_info:
  author: Miguel Alpanez
  description: >-
    Centralised log forwarding via rsyslog drop-in. Deploys
    /etc/rsyslog.d/99-forwarding.conf with RainerScript action() syntax,
    optional GnuTLS TLS, and disk-assisted queue. Supports review mode
    (audit-only) and enforce mode. NIS2 Art.21, SOC2 CC7.2, PCI-DSS 10.5.
  license: GPL-2.0-or-later
  min_ansible_version: "2.16"
  galaxy_tags:
    - security
    - hardening
    - rsyslog
    - logging
    - syslog
    - forwarding
    - tls
    - linux
    - pci
    - nis2
    - soc2
    - hipaa
    - compliance
  platforms:
    - name: Debian
      versions: [buster, bullseye, bookworm]
    - name: Ubuntu
      versions: [bionic, focal, jammy]
    - name: EL
      versions: [all]
dependencies: []
```

7. Write `roles/rsyslog_forwarding/meta/argument_specs.yml` with one entry per defaults variable (per STD-08). Each variable must have type, default, and description. Use the ntp_hardening argument_specs as the structural reference. Include all 16 variables from defaults/main.yml.

8. Write `roles/rsyslog_forwarding/handlers/main.yml` (per D-18):
```yaml
- name: Restart rsyslog
  ansible.builtin.systemd:
    name: "{{ rsyslog_forwarding_service }}"
    state: restarted
  changed_when: true
  when: not _rsyslog_in_container | bool
```

9. Write `roles/rsyslog_forwarding/tests/test.yml` (per STD-09):
```yaml
- name: Test rsyslog_forwarding role
  hosts: localhost
  gather_facts: true
  roles:
    - role: malpanez.security.rsyslog_forwarding
```

10. Reinstall collection (per STD-14):
```bash
ansible-galaxy collection install . --force -p .ansible/collections
```

11. Run lint on all new files:
```bash
yamllint roles/rsyslog_forwarding/
ansible-lint roles/rsyslog_forwarding/ --profile production
```
Fix any errors before completing.
  </action>
  <verify>
    <automated>ls roles/rsyslog_forwarding/defaults/main.yml roles/rsyslog_forwarding/vars/Debian.yml roles/rsyslog_forwarding/vars/RedHat.yml roles/rsyslog_forwarding/vars/Suse.yml roles/rsyslog_forwarding/meta/main.yml roles/rsyslog_forwarding/meta/argument_specs.yml roles/rsyslog_forwarding/handlers/main.yml roles/rsyslog_forwarding/tests/test.yml && yamllint roles/rsyslog_forwarding/ && ansible-lint roles/rsyslog_forwarding/ --profile production 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - roles/rsyslog_forwarding/defaults/main.yml contains `rsyslog_forwarding_enabled: false`
    - roles/rsyslog_forwarding/defaults/main.yml contains `rsyslog_forwarding_host: ""`
    - roles/rsyslog_forwarding/defaults/main.yml contains `rsyslog_forwarding_tls_enabled: false`
    - roles/rsyslog_forwarding/defaults/main.yml contains `rsyslog_forwarding_queue_type: LinkedList`
    - roles/rsyslog_forwarding/defaults/main.yml contains `rsyslog_forwarding_selector: "*.*"`
    - roles/rsyslog_forwarding/vars/Debian.yml contains `rsyslog_forwarding_tls_package: rsyslog-gnutls`
    - roles/rsyslog_forwarding/vars/Suse.yml contains `rsyslog_forwarding_tls_package: rsyslog-module-gtls`
    - roles/rsyslog_forwarding/vars/RedHat.yml contains `rsyslog_forwarding_service: rsyslog`
    - roles/rsyslog_forwarding/meta/main.yml contains `pci` in galaxy_tags
    - roles/rsyslog_forwarding/meta/main.yml contains `nis2` in galaxy_tags
    - roles/rsyslog_forwarding/meta/main.yml contains `versions: [all]` under EL
    - roles/rsyslog_forwarding/meta/argument_specs.yml contains `rsyslog_forwarding_host`
    - roles/rsyslog_forwarding/meta/argument_specs.yml contains `rsyslog_forwarding_tls_enabled`
    - roles/rsyslog_forwarding/handlers/main.yml contains `changed_when: true`
    - roles/rsyslog_forwarding/handlers/main.yml contains `not _rsyslog_in_container`
    - roles/rsyslog_forwarding/tests/test.yml contains `malpanez.security.rsyslog_forwarding`
    - yamllint roles/rsyslog_forwarding/ exits 0
    - ansible-lint roles/rsyslog_forwarding/ --profile production exits 0
  </acceptance_criteria>
  <done>Role scaffold exists with all 16 defaults variables, OS vars for 3 families, Galaxy-valid metadata with compliance tags, full argument_specs, container-aware handler, and FQCN test playbook. Lint passes.</done>
</task>

</tasks>

<verification>
- `ls roles/rsyslog_forwarding/{defaults,vars,meta,handlers,tests}/*.yml` — all files present
- `grep -c rsyslog_forwarding_ roles/rsyslog_forwarding/defaults/main.yml` — at least 16 variables
- `grep -c rsyslog_forwarding_ roles/rsyslog_forwarding/meta/argument_specs.yml` — at least 16 entries
- `grep 'rsyslog-module-gtls' roles/rsyslog_forwarding/vars/Suse.yml` — Suse TLS package correct
- `yamllint roles/rsyslog_forwarding/ && ansible-lint roles/rsyslog_forwarding/ --profile production` — 0 errors
</verification>

<success_criteria>
- Role directory created via ansible-galaxy role init
- All 16 defaults variables defined
- All 3 OS vars files have correct package names
- meta/main.yml passes Galaxy schema validation (EL versions: [all])
- argument_specs.yml has 1:1 coverage of defaults variables
- Handler uses changed_when: true with container guard
- tests/test.yml uses FQCN
- Collection reinstalled
- yamllint + ansible-lint 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-rsyslog-forwarding/03-01-SUMMARY.md`
</output>
