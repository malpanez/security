---
phase: 03-rsyslog-forwarding
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - roles/rsyslog_forwarding/tasks/main.yml
  - roles/rsyslog_forwarding/tasks/review.yml
  - roles/rsyslog_forwarding/tasks/enforce.yml
autonomous: true
requirements: [STD-02, STD-03, STD-04, STD-05, LOG-01, LOG-02, LOG-03, LOG-04, LOG-05]
must_haves:
  truths:
    - "main.yml gates enforce behind rsyslog_forwarding_enabled + security_mode == enforce"
    - "review.yml reports rsyslog status, drop-in presence, and TLS state without changing any files"
    - "enforce.yml deploys drop-in with rsyslogd -N1 validate, installs packages, manages service"
    - "TLS pre-flight assertions block anonymous auth mode, UDP protocol, and missing CA cert"
    - "Container detection skips service management and syntax checks"
  artifacts:
    - path: "roles/rsyslog_forwarding/tasks/main.yml"
      provides: "Standard gate pattern: setup, assert OS, include_vars, container detect, review/enforce"
      contains: "security_mode | default('review') == 'enforce'"
    - path: "roles/rsyslog_forwarding/tasks/review.yml"
      provides: "Read-only rsyslog state report"
      contains: "changed_when: false"
    - path: "roles/rsyslog_forwarding/tasks/enforce.yml"
      provides: "Package install, TLS assertions, template deploy with validate, service management"
      contains: "rsyslogd -N1 -f %s"
  key_links:
    - from: "roles/rsyslog_forwarding/tasks/main.yml"
      to: "roles/rsyslog_forwarding/tasks/enforce.yml"
      via: "include_tasks with when guard"
      pattern: "include_tasks.*enforce"
    - from: "roles/rsyslog_forwarding/tasks/enforce.yml"
      to: "roles/rsyslog_forwarding/templates/99-forwarding.conf.j2"
      via: "ansible.builtin.template with validate"
      pattern: "99-forwarding.conf.j2"
    - from: "roles/rsyslog_forwarding/tasks/enforce.yml"
      to: "roles/rsyslog_forwarding/handlers/main.yml"
      via: "notify: Restart rsyslog"
      pattern: "notify.*Restart rsyslog"
---

<objective>
Implement all three task files: main.yml (gate pattern), review.yml (read-only audit), and enforce.yml (install, configure, TLS assertions, service management).

Purpose: Core role logic that review audits rsyslog state and enforce deploys the forwarding drop-in.
Output: Complete task files ready for templates (plan 03-03) and molecule testing (plan 03-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-rsyslog-forwarding/03-CONTEXT.md
@.planning/phases/03-rsyslog-forwarding/03-RESEARCH.md
@.planning/phases/03-rsyslog-forwarding/03-01-SUMMARY.md

<interfaces>
<!-- From plan 03-01 scaffold — variables and OS vars the tasks consume -->

From roles/rsyslog_forwarding/defaults/main.yml (created in 03-01):
```yaml
rsyslog_forwarding_enabled: false
rsyslog_forwarding_host: ""
rsyslog_forwarding_port: 514
rsyslog_forwarding_protocol: tcp
rsyslog_forwarding_selector: "*.*"
rsyslog_forwarding_queue_type: LinkedList
rsyslog_forwarding_queue_size: 10000
rsyslog_forwarding_queue_spool_dir: /var/spool/rsyslog/forwarding
rsyslog_forwarding_queue_file_name: fwd-queue
rsyslog_forwarding_resume_retry_count: -1
rsyslog_forwarding_tls_enabled: false
rsyslog_forwarding_tls_ca_cert: ""
rsyslog_forwarding_tls_auth_mode: x509/name
rsyslog_forwarding_tls_client_cert: ""
rsyslog_forwarding_tls_client_key: ""
rsyslog_forwarding_logrotate_enabled: true
rsyslog_forwarding_logrotate_rotate: 4
rsyslog_forwarding_logrotate_size: 50M
rsyslog_forwarding_log_dir: /var/log
rsyslog_forwarding_file_create_mode: "0640"
```

From roles/rsyslog_forwarding/vars/Debian.yml:
```yaml
rsyslog_forwarding_package: rsyslog
rsyslog_forwarding_tls_package: rsyslog-gnutls
rsyslog_forwarding_service: rsyslog
rsyslog_forwarding_dropin_dir: /etc/rsyslog.d
```

From roles/rsyslog_forwarding/handlers/main.yml:
```yaml
- name: Restart rsyslog
  ansible.builtin.systemd:
    name: "{{ rsyslog_forwarding_service }}"
    state: restarted
  changed_when: true
  when: not _rsyslog_in_container | bool
```

<!-- Reference implementation from ntp_hardening -->
From roles/ntp_hardening/tasks/main.yml:
```yaml
- name: Gather minimum facts if not already collected
  ansible.builtin.setup:
    gather_subset: [min]
  when: ansible_facts == {}
  tags: [always]

- name: Assert supported OS family
  ansible.builtin.assert:
    that:
      - ansible_os_family in ['Debian', 'RedHat', 'Suse']
    fail_msg: >-
      ntp_hardening: unsupported OS family '{{ ansible_os_family }}'.
    quiet: true
  tags: [always]

- name: Load OS-specific variables
  ansible.builtin.include_vars: "{{ ansible_os_family }}.yml"
  tags: [always]

- name: Detect container environment
  ansible.builtin.set_fact:
    _ntp_in_container: >-
      {{ ansible_facts.virtualization_type | default('') in
         ['docker', 'podman', 'container', 'lxc'] }}
  tags: [always]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement tasks/main.yml gate pattern</name>
  <files>roles/rsyslog_forwarding/tasks/main.yml</files>
  <read_first>
    roles/rsyslog_forwarding/tasks/main.yml,
    roles/ntp_hardening/tasks/main.yml,
    roles/rsyslog_forwarding/defaults/main.yml
  </read_first>
  <action>
Write `roles/rsyslog_forwarding/tasks/main.yml` following the ntp_hardening pattern exactly (per STD-02, D-17). The file must contain exactly these 6 tasks in this order:

1. **Gather minimum facts** — `ansible.builtin.setup` with `gather_subset: [min]`, `when: ansible_facts == {}`, `tags: [always]`

2. **Assert supported OS family** — `ansible.builtin.assert` that `ansible_os_family in ['Debian', 'RedHat', 'Suse']`, fail_msg includes role name `rsyslog_forwarding`, `quiet: true`, `tags: [always]`

3. **Load OS-specific variables** — `ansible.builtin.include_vars: "{{ ansible_os_family }}.yml"`, `tags: [always]`

4. **Detect container environment** — `ansible.builtin.set_fact` setting `_rsyslog_in_container` to `{{ ansible_facts.virtualization_type | default('') in ['docker', 'podman', 'container', 'lxc'] }}`, `tags: [always]` (per D-17)

5. **Review rsyslog_forwarding configuration** — `ansible.builtin.include_tasks: review.yml`, `tags: [rsyslog_forwarding, review]`

6. **Enforce rsyslog_forwarding** — `ansible.builtin.include_tasks: enforce.yml`, with `when:` list containing `rsyslog_forwarding_enabled | bool` AND `security_mode | default('review') == 'enforce'`, `tags: [rsyslog_forwarding, enforce]`

All task names must start uppercase. All modules must use FQCN (per STD-05).
  </action>
  <verify>
    <automated>grep -c 'ansible.builtin' roles/rsyslog_forwarding/tasks/main.yml && grep "default('review')" roles/rsyslog_forwarding/tasks/main.yml && grep '_rsyslog_in_container' roles/rsyslog_forwarding/tasks/main.yml</automated>
  </verify>
  <acceptance_criteria>
    - roles/rsyslog_forwarding/tasks/main.yml contains `ansible.builtin.setup`
    - roles/rsyslog_forwarding/tasks/main.yml contains `ansible_os_family in ['Debian', 'RedHat', 'Suse']`
    - roles/rsyslog_forwarding/tasks/main.yml contains `ansible.builtin.include_vars: "{{ ansible_os_family }}.yml"`
    - roles/rsyslog_forwarding/tasks/main.yml contains `_rsyslog_in_container`
    - roles/rsyslog_forwarding/tasks/main.yml contains `security_mode | default('review') == 'enforce'`
    - roles/rsyslog_forwarding/tasks/main.yml contains `rsyslog_forwarding_enabled | bool`
    - roles/rsyslog_forwarding/tasks/main.yml contains `include_tasks: review.yml`
    - roles/rsyslog_forwarding/tasks/main.yml contains `include_tasks: enforce.yml`
    - All task names start uppercase
  </acceptance_criteria>
  <done>tasks/main.yml follows the ntp_hardening gate pattern with rsyslog-specific container fact and review/enforce gate.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tasks/review.yml and tasks/enforce.yml</name>
  <files>
    roles/rsyslog_forwarding/tasks/review.yml,
    roles/rsyslog_forwarding/tasks/enforce.yml
  </files>
  <read_first>
    roles/ntp_hardening/tasks/review.yml,
    roles/ntp_hardening/tasks/enforce.yml,
    roles/rsyslog_forwarding/defaults/main.yml,
    roles/rsyslog_forwarding/vars/Debian.yml,
    roles/rsyslog_forwarding/handlers/main.yml
  </read_first>
  <action>
**review.yml** (per LOG-01, STD-03 — read-only only, all tasks changed_when: false):

Write `roles/rsyslog_forwarding/tasks/review.yml` with these tasks:

1. **Review | Check rsyslog package (RedHat/Suse)** — `ansible.builtin.command: rpm -q rsyslog`, `register: _rsyslog_pkg_check`, `changed_when: false`, `failed_when: false`, `when: ansible_os_family in ['RedHat', 'Suse']`, `tags: [rsyslog_forwarding, review]`

2. **Review | Check rsyslog package (Debian)** — `ansible.builtin.command: dpkg-query -W rsyslog`, `register: _rsyslog_pkg_check`, `changed_when: false`, `failed_when: false`, `when: ansible_os_family == 'Debian'`, `tags: [rsyslog_forwarding, review]`

3. **Review | Check rsyslog service status** — `ansible.builtin.command: systemctl is-active rsyslog`, `register: _rsyslog_svc_check`, `changed_when: false`, `failed_when: false`, `tags: [rsyslog_forwarding, review]`

4. **Review | Check forwarding drop-in exists** — `ansible.builtin.stat: path: "{{ rsyslog_forwarding_dropin_dir }}/99-forwarding.conf"`, `register: _rsyslog_dropin_stat`, `tags: [rsyslog_forwarding, review]`

5. **Review | Read drop-in content** — `ansible.builtin.slurp: src: "{{ rsyslog_forwarding_dropin_dir }}/99-forwarding.conf"`, `register: _rsyslog_dropin_content`, `when: _rsyslog_dropin_stat.stat.exists`, `tags: [rsyslog_forwarding, review]`

6. **Review | Detect TLS in drop-in** — `ansible.builtin.set_fact: _rsyslog_tls_present: "{{ 'StreamDriverMode' in (_rsyslog_dropin_content.content | default('') | b64decode) }}"`, `when: _rsyslog_dropin_stat.stat.exists`, `tags: [rsyslog_forwarding, review]`

7. **Review | Report rsyslog_forwarding findings** — `ansible.builtin.debug` with msg list showing: host, rsyslog installed (rc==0), forwarding drop-in exists, service status, TLS configured. `tags: [rsyslog_forwarding, review]`

**enforce.yml** (per LOG-02, LOG-03, LOG-04, LOG-05, STD-04, D-12 through D-16):

Write `roles/rsyslog_forwarding/tasks/enforce.yml` with these tagged sections:

**Pre-flight assertions** (tags: [rsyslog_forwarding, enforce, configure]):
1. **Enforce | Assert forwarding host is defined** — assert `rsyslog_forwarding_host | length > 0`, fail_msg: `rsyslog_forwarding_host must be set to a non-empty string.` (per LOG-05)

2. **Enforce | Assert TLS protocol is tcp** — assert `rsyslog_forwarding_protocol == 'tcp'`, fail_msg: `TLS requires protocol=tcp; UDP is not supported by GnuTLS.`, `when: rsyslog_forwarding_tls_enabled | bool` (per D-13)

3. **Enforce | Assert TLS auth mode is not anonymous** — assert `rsyslog_forwarding_tls_auth_mode != 'anonymous'`, fail_msg: `anonymous TLS auth mode is explicitly blocked.`, `when: rsyslog_forwarding_tls_enabled | bool` (per D-12)

4. **Enforce | Stat CA cert on managed host** — `ansible.builtin.stat: path: "{{ rsyslog_forwarding_tls_ca_cert }}"`, `register: _rsyslog_ca_cert_stat`, `when: rsyslog_forwarding_tls_enabled | bool` (per D-14)

5. **Enforce | Assert CA cert exists** — assert `_rsyslog_ca_cert_stat.stat.exists`, fail_msg referencing the path, `when: rsyslog_forwarding_tls_enabled | bool` (per D-14)

**Install section** (tags: [rsyslog_forwarding, enforce, install]):
6. **Enforce | Install rsyslog (RedHat)** — `ansible.builtin.dnf: name: "{{ rsyslog_forwarding_package }}", state: present`, `when: ansible_os_family == 'RedHat'`

7. **Enforce | Install rsyslog (Debian)** — `ansible.builtin.apt: name: "{{ rsyslog_forwarding_package }}", state: present, cache_valid_time: 3600`, `when: ansible_os_family == 'Debian'`

8. **Enforce | Install rsyslog (Suse)** — `community.general.zypper: name: "{{ rsyslog_forwarding_package }}", state: present`, `when: ansible_os_family == 'Suse'`

9. **Enforce | Install rsyslog TLS package (RedHat)** — `ansible.builtin.dnf: name: "{{ rsyslog_forwarding_tls_package }}", state: present`, `when: ansible_os_family == 'RedHat' and rsyslog_forwarding_tls_enabled | bool`

10. **Enforce | Install rsyslog TLS package (Debian)** — `ansible.builtin.apt: name: "{{ rsyslog_forwarding_tls_package }}", state: present`, `when: ansible_os_family == 'Debian' and rsyslog_forwarding_tls_enabled | bool`

11. **Enforce | Install rsyslog TLS package (Suse)** — `community.general.zypper: name: "{{ rsyslog_forwarding_tls_package }}", state: present`, `when: ansible_os_family == 'Suse' and rsyslog_forwarding_tls_enabled | bool`

**Configure section** (tags: [rsyslog_forwarding, enforce, configure]):
12. **Enforce | Ensure drop-in directory exists** — `ansible.builtin.file: path: "{{ rsyslog_forwarding_dropin_dir }}", state: directory, owner: root, group: root, mode: "0755"`

13. **Enforce | Deploy forwarding drop-in** — `ansible.builtin.template: src: 99-forwarding.conf.j2, dest: "{{ rsyslog_forwarding_dropin_dir }}/99-forwarding.conf", owner: root, group: root, mode: "0644", validate: "rsyslogd -N1 -f %s", backup: true`, `notify: Restart rsyslog` (per LOG-02, D-15, D-16)

14. **Enforce | Deploy logrotate stanza** — `ansible.builtin.template: src: rsyslog-logrotate.j2, dest: /etc/logrotate.d/rsyslog-forwarding, owner: root, group: root, mode: "0644"`, `when: rsyslog_forwarding_logrotate_enabled | bool` (per D-07, D-09)

**Service section** (tags: [rsyslog_forwarding, enforce, service]):
15. **Enforce | Enable and start rsyslog** — `ansible.builtin.systemd: name: "{{ rsyslog_forwarding_service }}", state: started, enabled: true, daemon_reload: true`, `when: not _rsyslog_in_container | bool`

**Verify section** (tags: [rsyslog_forwarding, enforce, verify]):
16. **Enforce | Run rsyslogd syntax check** — `ansible.builtin.command: rsyslogd -N1 -f {{ rsyslog_forwarding_dropin_dir }}/99-forwarding.conf`, `register: _rsyslog_syntax_check`, `changed_when: false`, `failed_when: _rsyslog_syntax_check.rc != 0`, `when: not _rsyslog_in_container | bool`

17. **Enforce | Assert rsyslog service is active** — `ansible.builtin.command: systemctl is-active {{ rsyslog_forwarding_service }}`, `register: _rsyslog_svc_active`, `changed_when: false`, `failed_when: _rsyslog_svc_active.stdout != 'active'`, `when: not _rsyslog_in_container | bool`

All task names start uppercase. All modules use FQCN. All assert tasks use `quiet: true`.
  </action>
  <verify>
    <automated>grep -c 'changed_when: false' roles/rsyslog_forwarding/tasks/review.yml && grep 'rsyslogd -N1' roles/rsyslog_forwarding/tasks/enforce.yml && grep "anonymous" roles/rsyslog_forwarding/tasks/enforce.yml && yamllint roles/rsyslog_forwarding/tasks/ && ansible-lint roles/rsyslog_forwarding/ --profile production 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - roles/rsyslog_forwarding/tasks/review.yml contains at least 4 tasks with `changed_when: false`
    - roles/rsyslog_forwarding/tasks/review.yml contains `StreamDriverMode`
    - roles/rsyslog_forwarding/tasks/review.yml contains `ansible.builtin.slurp`
    - roles/rsyslog_forwarding/tasks/review.yml does NOT contain any template, copy, file, or lineinfile tasks
    - roles/rsyslog_forwarding/tasks/enforce.yml contains `rsyslog_forwarding_host | length > 0`
    - roles/rsyslog_forwarding/tasks/enforce.yml contains `rsyslog_forwarding_protocol == 'tcp'`
    - roles/rsyslog_forwarding/tasks/enforce.yml contains `rsyslog_forwarding_tls_auth_mode != 'anonymous'`
    - roles/rsyslog_forwarding/tasks/enforce.yml contains `rsyslogd -N1 -f %s`
    - roles/rsyslog_forwarding/tasks/enforce.yml contains `notify: Restart rsyslog`
    - roles/rsyslog_forwarding/tasks/enforce.yml contains `99-forwarding.conf.j2`
    - roles/rsyslog_forwarding/tasks/enforce.yml contains `rsyslog-logrotate.j2`
    - roles/rsyslog_forwarding/tasks/enforce.yml contains `tags:` with install, configure, service, verify sections
    - yamllint roles/rsyslog_forwarding/tasks/ exits 0
    - ansible-lint roles/rsyslog_forwarding/ --profile production exits 0
  </acceptance_criteria>
  <done>All three task files implement the review/enforce pattern with TLS pre-flight assertions, template deploy with validate, and container-aware service management. Lint passes.</done>
</task>

</tasks>

<verification>
- `grep -c 'ansible.builtin' roles/rsyslog_forwarding/tasks/main.yml` — at least 5 FQCN uses
- `grep -c 'changed_when: false' roles/rsyslog_forwarding/tasks/review.yml` — at least 3 read-only guards
- `grep 'rsyslogd -N1 -f %s' roles/rsyslog_forwarding/tasks/enforce.yml` — template validate present
- `grep 'anonymous' roles/rsyslog_forwarding/tasks/enforce.yml` — D-12 assertion present
- `yamllint roles/rsyslog_forwarding/tasks/ && ansible-lint roles/rsyslog_forwarding/ --profile production` — 0 errors
</verification>

<success_criteria>
- main.yml follows exact ntp_hardening gate pattern with _rsyslog_in_container
- review.yml is purely read-only (no file modifications, all changed_when: false)
- enforce.yml has 4 tagged sections: install, configure, service, verify
- TLS pre-flight assertions block anonymous auth, UDP protocol, missing CA cert
- Template task uses validate: rsyslogd -N1 -f %s
- All modules use FQCN, all task names start uppercase
- yamllint + ansible-lint 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-rsyslog-forwarding/03-02-SUMMARY.md`
</output>
