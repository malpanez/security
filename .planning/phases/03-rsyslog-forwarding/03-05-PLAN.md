---
phase: 03-rsyslog-forwarding
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - .github/workflows/ci-uv.yml
  - .github/workflows/ci-cd-enterprise.yml
autonomous: true
requirements: [STD-12, STD-13]
must_haves:
  truths:
    - "rsyslog_forwarding appears in ci-uv.yml test-roles matrix"
    - "rsyslog_forwarding appears in ci-cd-enterprise.yml test-molecule matrix"
    - "yamllint and ansible-lint --profile production pass with 0 errors on the full role"
  artifacts:
    - path: ".github/workflows/ci-uv.yml"
      provides: "CI test-roles matrix entry"
      contains: "rsyslog_forwarding"
    - path: ".github/workflows/ci-cd-enterprise.yml"
      provides: "CI test-molecule matrix entry"
      contains: "rsyslog_forwarding"
  key_links:
    - from: ".github/workflows/ci-uv.yml"
      to: "roles/rsyslog_forwarding/"
      via: "test-roles matrix entry triggers molecule test"
      pattern: "rsyslog_forwarding"
---

<objective>
Add rsyslog_forwarding to both CI workflow matrices and verify full role passes yamllint + ansible-lint.

Purpose: Enable CI coverage for the new role.
Output: CI matrices updated, lint passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-rsyslog-forwarding/03-CONTEXT.md

<interfaces>
<!-- CI matrix structure — executor must add entry after user_audit -->

From .github/workflows/ci-uv.yml (lines 132-135):
```yaml
          - grype_scanner
          - aide
          - ntp_hardening
          - user_audit
```

From .github/workflows/ci-cd-enterprise.yml (lines 335-338):
```yaml
          - grype_scanner
          - aide
          - ntp_hardening
          - user_audit
```

The new entry `- rsyslog_forwarding` goes immediately after `- user_audit` in both files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rsyslog_forwarding to CI matrices and run lint</name>
  <files>
    .github/workflows/ci-uv.yml,
    .github/workflows/ci-cd-enterprise.yml
  </files>
  <read_first>
    .github/workflows/ci-uv.yml,
    .github/workflows/ci-cd-enterprise.yml
  </read_first>
  <action>
1. In `.github/workflows/ci-uv.yml`, find the test-roles matrix list. Locate the line `- user_audit` and add `- rsyslog_forwarding` on the next line, maintaining the same indentation.

2. In `.github/workflows/ci-cd-enterprise.yml`, find the test-molecule matrix list. Locate the line `- user_audit` and add `- rsyslog_forwarding` on the next line, maintaining the same indentation.

3. Reinstall the collection to ensure all role files are recognized:
```bash
ansible-galaxy collection install . --force -p .ansible/collections
```

4. Run full lint on the role:
```bash
yamllint roles/rsyslog_forwarding/
ansible-lint roles/rsyslog_forwarding/ --profile production
```
Fix any errors. Common issues: task names not starting uppercase, missing FQCN, changed_when not set on command tasks.

5. Verify CI YAML is valid:
```bash
yamllint .github/workflows/ci-uv.yml
yamllint .github/workflows/ci-cd-enterprise.yml
```
  </action>
  <verify>
    <automated>grep 'rsyslog_forwarding' .github/workflows/ci-uv.yml && grep 'rsyslog_forwarding' .github/workflows/ci-cd-enterprise.yml && yamllint roles/rsyslog_forwarding/ && ansible-lint roles/rsyslog_forwarding/ --profile production 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci-uv.yml contains `- rsyslog_forwarding` in the test-roles matrix
    - .github/workflows/ci-cd-enterprise.yml contains `- rsyslog_forwarding` in the test-molecule matrix
    - `rsyslog_forwarding` appears after `user_audit` in both files
    - yamllint roles/rsyslog_forwarding/ exits 0
    - ansible-lint roles/rsyslog_forwarding/ --profile production exits 0
    - yamllint .github/workflows/ci-uv.yml exits 0
    - yamllint .github/workflows/ci-cd-enterprise.yml exits 0
  </acceptance_criteria>
  <done>rsyslog_forwarding added to both CI matrices. Full role passes yamllint and ansible-lint --profile production with 0 errors.</done>
</task>

</tasks>

<verification>
- `grep -c 'rsyslog_forwarding' .github/workflows/ci-uv.yml` — at least 1
- `grep -c 'rsyslog_forwarding' .github/workflows/ci-cd-enterprise.yml` — at least 1
- `yamllint roles/rsyslog_forwarding/ && ansible-lint roles/rsyslog_forwarding/ --profile production` — 0 errors
</verification>

<success_criteria>
- rsyslog_forwarding in ci-uv.yml test-roles matrix
- rsyslog_forwarding in ci-cd-enterprise.yml test-molecule matrix
- Full role passes yamllint + ansible-lint with 0 errors
- CI workflow YAML passes yamllint
</success_criteria>

<output>
After completion, create `.planning/phases/03-rsyslog-forwarding/03-05-SUMMARY.md`
</output>
