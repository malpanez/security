---
plan: "02-05: CI integration"
phase: 2
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - .github/workflows/ci-uv.yml
  - .github/workflows/ci-cd-enterprise.yml
requirements_addressed: [STD-13]
autonomous: true
must_haves:
  truths:
    - "user_audit appears in ci-uv.yml test-roles matrix"
    - "user_audit appears in ci-cd-enterprise.yml test-molecule matrix"
    - "yamllint and ansible-lint --profile production pass with 0 errors on the full role"
    - "Collection reinstalled after role addition"
  artifacts:
    - path: ".github/workflows/ci-uv.yml"
      provides: "CI workflow with user_audit in test matrix"
      contains: "user_audit"
    - path: ".github/workflows/ci-cd-enterprise.yml"
      provides: "Enterprise CI with user_audit in molecule matrix"
      contains: "user_audit"
  key_links:
    - from: ".github/workflows/ci-uv.yml"
      to: "roles/user_audit/molecule/default/"
      via: "matrix.role triggers molecule test on user_audit"
      pattern: "user_audit"
---

## Objective

Add user_audit to CI workflow matrices and run final lint validation on the entire role.

Purpose: Ensure user_audit is tested in CI on every push/PR, matching all other roles in the collection.
Output: Updated ci-uv.yml and ci-cd-enterprise.yml with user_audit in their test matrices.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-audit/02-01-SUMMARY.md

## Tasks

<task type="auto">
  <name>Task 1: Add user_audit to CI matrices and run final lint</name>
  <read_first>
    - .github/workflows/ci-uv.yml
    - .github/workflows/ci-cd-enterprise.yml
  </read_first>
  <files>
    .github/workflows/ci-uv.yml
    .github/workflows/ci-cd-enterprise.yml
  </files>
  <action>
1. **Add user_audit to ci-uv.yml test-roles matrix:**

Read `.github/workflows/ci-uv.yml`. Find the `test-roles:` job, locate the `matrix: role:` list, and add `- user_audit` after the last entry in the list. Use the same indentation as existing entries.

Do NOT add to `test-roles-slow` — user_audit has no long-running operations (no package downloads, no service waits).

2. **Add user_audit to ci-cd-enterprise.yml test-molecule matrix:**

Read `.github/workflows/ci-cd-enterprise.yml`. Find the `test-molecule:` job, locate the `matrix: role:` list, and add `- user_audit` after the last entry in the list.

3. **Reinstall collection** (required after adding new role to ensure CI can resolve FQCN):
```bash
ansible-galaxy collection install . --force -p .ansible/collections
```

4. **Run final lint validation** on the entire role:
```bash
yamllint roles/user_audit/
ansible-lint --profile production roles/user_audit/
```

Both must exit 0 with no errors. If ansible-lint reports issues, fix them in the role files (most likely: task name casing, shell pipefail, changed_when missing).

5. **Verify no secrets exposure:** user_audit handles no secrets (chage, lastlog, pwck are non-secret operations). Confirm no `no_log: true` is needed by reviewing task output — all data is already in /etc/passwd and /etc/shadow (shadow is read only by root, and Ansible runs as root).

Key constraints:
- Do NOT modify any other CI workflow file
- Do NOT add user_audit to docker-test.yml or molecule-test.yml (those use different patterns)
- Preserve exact YAML formatting and indentation of existing entries
  </action>
  <verify>
    <automated>
grep -q 'user_audit' .github/workflows/ci-uv.yml && \
grep -q 'user_audit' .github/workflows/ci-cd-enterprise.yml && \
yamllint roles/user_audit/ && \
ansible-lint --profile production roles/user_audit/ && \
echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `user_audit` appears in ci-uv.yml test-roles matrix (grep confirms)
    - `user_audit` appears in ci-cd-enterprise.yml test-molecule matrix (grep confirms)
    - `yamllint roles/user_audit/` exits 0
    - `ansible-lint --profile production roles/user_audit/` exits 0
    - Collection reinstalled
    - No other CI files modified
  </acceptance_criteria>
  <done>user_audit integrated into CI. yamllint and ansible-lint pass with 0 errors.</done>
</task>

## Verification

```bash
grep 'user_audit' .github/workflows/ci-uv.yml .github/workflows/ci-cd-enterprise.yml && \
yamllint roles/user_audit/ && \
ansible-lint --profile production roles/user_audit/ && \
echo "Plan 02-05 verified"
```

## Success Criteria

- user_audit present in both CI workflow matrices
- Full role passes yamllint + ansible-lint --profile production with 0 errors
- Collection reinstalled successfully

## Output

After completion, create `.planning/phases/02-user-audit/02-05-SUMMARY.md`
