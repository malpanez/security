---
phase: 05.1-ssh-access-vm-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ssh-access-vm-test.yml
  - playbooks/ssh-access-vm-test.yml
  - roles/user_audit/tests/inventory
  - .planning/ROADMAP.md
autonomous: true
requirements: [VMTEST-01, VMTEST-02, VMTEST-03, VMTEST-04]

must_haves:
  truths:
    - "ssh-access-vm-test.yml workflow is valid YAML and uses correct runner labels"
    - "ssh-access-vm-test.yml playbook passes ansible --syntax-check"
    - "user_audit/tests/inventory contains localhost"
    - "All 3 files are committed to develop branch"
    - "ROADMAP.md phase 05.1 has a real goal (not placeholder)"
  artifacts:
    - path: ".github/workflows/ssh-access-vm-test.yml"
      provides: "GitHub Actions workflow for VM-based SSH access control tests"
    - path: "playbooks/ssh-access-vm-test.yml"
      provides: "Ansible playbook for VM SSH access tests"
    - path: "roles/user_audit/tests/inventory"
      provides: "Test inventory with localhost"
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase 05.1 goal and plan list"
  key_links:
    - from: ".github/workflows/ssh-access-vm-test.yml"
      to: "playbooks/ssh-access-vm-test.yml"
      via: "ansible-playbook command in workflow step"
      pattern: "playbooks/ssh-access-vm-test.yml"
    - from: "playbooks/ssh-access-vm-test.yml"
      to: "roles/sssd_ad_integration"
      via: "role reference in playbook"
      pattern: "malpanez.security.sssd_ad_integration"
---

<objective>
Validate, lint, and commit three untracked files that enable VM-based SSH access control testing for the sssd_ad_integration role, then update ROADMAP.md.

Purpose: The sssd_ad_integration role needs real sshd/sudoers/pam_access testing that containers cannot provide. These files enable that via GitHub-hosted Ubuntu VMs using local groups to simulate AD membership.

Output: Three files committed to develop, ROADMAP.md updated with phase 05.1 details.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ansible.cfg
@.yamllint.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate and lint the 3 untracked files</name>
  <files>
    .github/workflows/ssh-access-vm-test.yml
    playbooks/ssh-access-vm-test.yml
    roles/user_audit/tests/inventory
  </files>
  <read_first>
    .github/workflows/ssh-access-vm-test.yml
    playbooks/ssh-access-vm-test.yml
    roles/user_audit/tests/inventory
    .yamllint.yml
    ansible.cfg
  </read_first>
  <action>
    Validate all 3 files before committing. Per VMTEST-01 and VMTEST-02:

    1. Run `yamllint -c .yamllint.yml playbooks/ssh-access-vm-test.yml` — the playbook YAML must pass yamllint.
       Note: .github/ is in yamllint ignore list, so the workflow file is NOT checked by yamllint (this is expected and correct).

    2. Run `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ssh-access-vm-test.yml'))"` to verify the workflow YAML is parseable.

    3. Run `ansible-galaxy collection install . --force -p .ansible/collections` first (self-collection needed for syntax check).
       Then run `ANSIBLE_COLLECTIONS_PATH=.ansible/collections ansible-playbook playbooks/ssh-access-vm-test.yml --syntax-check` — must exit 0.

    4. Verify the workflow uses runner labels that exist in this org:
       - `ubuntu-22.04` and `ubuntu-24.04` for the test matrix — standard GitHub-hosted runners, valid.
       - `ubuntu-slim` for the summary job — used in branch-management.yml already, valid for this org.

    5. Verify `roles/user_audit/tests/inventory` contains `localhost` on line 1.

    6. Verify the workflow references `playbooks/ssh-access-vm-test.yml` in its ansible-playbook command (key link between workflow and playbook).

    7. Verify the playbook references `malpanez.security.sssd_ad_integration` as FQCN role (key link between playbook and role).

    If any lint or syntax check fails, fix the issue in the file before proceeding. Likely fixes:
    - Trailing whitespace: remove it
    - Line length > 120: break the line
    - Missing document start `---`: add it
    Do NOT fix anything that changes behavior — only formatting/lint issues.
  </action>
  <verify>
    <automated>yamllint -c .yamllint.yml playbooks/ssh-access-vm-test.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ssh-access-vm-test.yml'))" && ANSIBLE_COLLECTIONS_PATH=.ansible/collections ansible-playbook playbooks/ssh-access-vm-test.yml --syntax-check</automated>
  </verify>
  <acceptance_criteria>
    - `yamllint -c .yamllint.yml playbooks/ssh-access-vm-test.yml` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ssh-access-vm-test.yml'))"` exits 0
    - `ansible-playbook playbooks/ssh-access-vm-test.yml --syntax-check` exits 0
    - `grep -c 'localhost' roles/user_audit/tests/inventory` returns at least 1
    - `grep 'playbooks/ssh-access-vm-test.yml' .github/workflows/ssh-access-vm-test.yml` finds the reference
    - `grep 'malpanez.security.sssd_ad_integration' playbooks/ssh-access-vm-test.yml` finds the FQCN
  </acceptance_criteria>
  <done>All 3 files pass lint and syntax validation. No behavioral changes made. Files are ready to commit.</done>
</task>

<task type="auto">
  <name>Task 2: Commit files and update ROADMAP.md</name>
  <files>
    .github/workflows/ssh-access-vm-test.yml
    playbooks/ssh-access-vm-test.yml
    roles/user_audit/tests/inventory
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/ROADMAP.md
  </read_first>
  <action>
    Per VMTEST-03 and VMTEST-04:

    1. Update ROADMAP.md phase 05.1 section. Replace the current placeholder content:
       ```
       **Goal:** [Urgent work - to be planned]
       **Requirements**: TBD
       **Depends on:** Phase 5
       **Plans:** 0 plans

       Plans:
       - [ ] TBD (run /gsd:plan-phase 05.1 to break down)
       ```
       With:
       ```
       **Goal:** Commit and validate VM-based SSH access control test workflow, playbook, and user_audit test inventory for sssd_ad_integration structural validation (real sshd, sudoers, pam_access; no AD DC required).
       **Requirements**: VMTEST-01, VMTEST-02, VMTEST-03, VMTEST-04
       **Depends on:** Phase 5
       **Plans:** 1 plan

       Plans:
       - [ ] 05.1-01-PLAN.md -- Validate, lint, commit 3 untracked files and update ROADMAP
       ```

    2. Commit the 3 untracked files with a focused commit message:
       ```
       git add .github/workflows/ssh-access-vm-test.yml playbooks/ssh-access-vm-test.yml roles/user_audit/tests/inventory
       git commit -m "ci(sssd_ad_integration): add VM-based SSH access control test workflow

       Adds GitHub Actions workflow running on ubuntu-22.04/24.04 VMs to test
       sssd_ad_integration enforce mode with real sshd, sudoers, and pam_access.
       Local groups simulate AD membership (no DC required).

       Includes:
       - .github/workflows/ssh-access-vm-test.yml (workflow)
       - playbooks/ssh-access-vm-test.yml (test playbook)
       - roles/user_audit/tests/inventory (test inventory)"
       ```

    3. Commit the ROADMAP.md update separately:
       ```
       git add .planning/ROADMAP.md
       git commit -m "docs(05.1): update ROADMAP phase 05.1 goal and plan list"
       ```
  </action>
  <verify>
    <automated>git log --oneline -2 && git diff HEAD~2 --name-only && grep 'VMTEST-01' .planning/ROADMAP.md && grep '05.1-01-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -2` shows two new commits (one for files, one for ROADMAP)
    - `git diff HEAD~2 --name-only` includes all 4 files
    - `grep 'VMTEST-01' .planning/ROADMAP.md` finds the requirement IDs in phase 05.1
    - `grep '05.1-01-PLAN.md' .planning/ROADMAP.md` finds the plan reference
    - `grep '\[Urgent work' .planning/ROADMAP.md` returns NO matches (placeholder removed)
    - `git status` shows no untracked files for the 3 committed files
  </acceptance_criteria>
  <done>All 3 files committed to develop. ROADMAP.md updated with real goal, requirement IDs, and plan reference. No placeholder text remains in phase 05.1.</done>
</task>

</tasks>

<verification>
1. `yamllint -c .yamllint.yml playbooks/ssh-access-vm-test.yml` exits 0
2. `ansible-playbook playbooks/ssh-access-vm-test.yml --syntax-check` exits 0
3. `git log --oneline -3` shows the new commits
4. `grep 'VMTEST' .planning/ROADMAP.md` shows requirement IDs in phase 05.1
5. `git status` shows clean working tree for the 3 committed files
</verification>

<success_criteria>
- Three files (.github/workflows/ssh-access-vm-test.yml, playbooks/ssh-access-vm-test.yml, roles/user_audit/tests/inventory) pass validation and are committed to develop
- ROADMAP.md phase 05.1 has real goal, requirements, and plan list (no placeholders)
- No lint errors introduced, no behavioral changes to the files
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-ssh-access-vm-test-commit-workflow-playbook-and-user-audit-inventory-for-vm-based-sssd-ad-integration-validation-real-sshd-sudoers-pam-access-no-ad-dc-required/05.1-01-SUMMARY.md`
</output>
