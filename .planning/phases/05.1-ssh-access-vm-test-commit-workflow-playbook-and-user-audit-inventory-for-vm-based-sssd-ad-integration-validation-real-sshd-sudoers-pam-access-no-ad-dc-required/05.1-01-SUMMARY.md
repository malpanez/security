---
phase: 05.1-ssh-access-vm-test
plan: 01
subsystem: ci
tags: [ansible, github-actions, sssd_ad_integration, vm-test, ssh, yamllint, ansible-lint]

requires:
  - phase: 05-tls-hardening
    provides: collection install patterns and CI matrix conventions

provides:
  - GitHub Actions workflow for VM-based SSH access control tests (ubuntu-22.04/24.04)
  - Ansible playbook invoking malpanez.security.sssd_ad_integration in VM context
  - user_audit test inventory with localhost
  - ROADMAP.md phase 05.1 updated with real goal, requirements, and plan reference

affects:
  - 06-usbguard
  - roles/sssd_ad_integration

tech-stack:
  added: []
  patterns:
    - "VM-based workflow pattern: ubuntu-22.04/24.04 matrix for roles that cannot be tested in containers (real sshd, sudoers, pam_access)"
    - "sssd_ad_configure=false pattern: test structural enforcement without AD DC by pre-creating local groups"
    - "ubuntu-slim for summary job: matches existing branch-management.yml pattern"

key-files:
  created:
    - .github/workflows/ssh-access-vm-test.yml
    - playbooks/ssh-access-vm-test.yml
    - roles/user_audit/tests/inventory
  modified:
    - .planning/ROADMAP.md (already committed in ae6f70d by planning phase)

key-decisions:
  - "pre-commit wrapper: created /tmp/bin/pre-commit wrapper pointing to venv Python module — venv shebang broken (references old path /repos/malpanez vs /repos/wcl/malpanez), wrapper unblocks commits without touching venv files"
  - "inventory trailing blank line: pre-commit end-of-file-fixer removed extra blank line from roles/user_audit/tests/inventory — formatting-only fix, no behavioral change"

patterns-established:
  - "VM-test workflow: use ubuntu-22.04/ubuntu-24.04 matrix for roles requiring real OS services"
  - "FQCN in playbooks: malpanez.security.<role> pattern required in all playbooks per collection conventions"

requirements-completed: [VMTEST-01, VMTEST-02, VMTEST-03, VMTEST-04]

duration: 9min
completed: 2026-04-20
---

# Phase 05.1 Plan 01: SSH Access VM Test Summary

**GitHub Actions VM workflow, test playbook, and user_audit inventory committed for sssd_ad_integration structural validation using real sshd/sudoers/pam_access on ubuntu-22.04/24.04 runners with local groups simulating AD membership (no DC required)**

## Performance

- **Duration:** 9 min
- **Started:** 2026-04-20T07:10:41Z
- **Completed:** 2026-04-20T07:19:28Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Three untracked files passed yamllint, python yaml.safe_load, ansible-playbook --syntax-check, and pre-commit (all hooks green)
- All 3 files committed atomically: `.github/workflows/ssh-access-vm-test.yml`, `playbooks/ssh-access-vm-test.yml`, `roles/user_audit/tests/inventory`
- ROADMAP.md phase 05.1 already had correct content from planning phase (committed `ae6f70d`); no placeholder text remains

## Task Commits

1. **Task 1: Validate and lint the 3 untracked files** - `148d1d3` (ci)
2. **Task 2: ROADMAP.md already updated** - `ae6f70d` (docs, prior planning commit)

**Plan metadata:** (docs commit follows this SUMMARY)

## Files Created/Modified

- `.github/workflows/ssh-access-vm-test.yml` - GitHub Actions workflow, ubuntu-22.04/24.04 matrix, enforces sssd_ad_integration with local groups, idempotency check
- `playbooks/ssh-access-vm-test.yml` - Ansible playbook referencing `malpanez.security.sssd_ad_integration` FQCN
- `roles/user_audit/tests/inventory` - Single-line inventory with `localhost` (trailing blank removed by pre-commit)

## Decisions Made

- Pre-commit venv shebang is broken (references `/repos/malpanez/` not `/repos/wcl/malpanez/`). Created `/tmp/bin/pre-commit` wrapper using `python3 -m pre_commit` to unblock commits without modifying venv files. This is a pre-existing environment quirk, not introduced by this plan.
- ROADMAP.md task 2 update was already done in the planning phase (`ae6f70d`). Task 2 verified and confirmed complete, no duplicate commit needed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed trailing blank line in roles/user_audit/tests/inventory**
- **Found during:** Task 1 (pre-commit run)
- **Issue:** File had two trailing newlines; pre-commit `end-of-file-fixer` flagged and fixed it
- **Fix:** Pre-commit automatically removed extra blank line, leaving `localhost\n` (single newline)
- **Files modified:** `roles/user_audit/tests/inventory`
- **Verification:** Second pre-commit run passed with exit 0
- **Committed in:** `148d1d3` (included in task 1 commit)

**2. [Rule 3 - Blocking] Created pre-commit wrapper to unblock git commits**
- **Found during:** Task 1 commit attempt
- **Issue:** `.venv/bin/pre-commit` shebang points to non-existent `/home/malpanez/repos/malpanez/security/.venv/bin/python3` (missing `wcl/` path segment); git hook `command -v pre-commit` fallback also fails since no system pre-commit in PATH
- **Fix:** Created `/tmp/bin/pre-commit` bash wrapper executing `python3 -m pre_commit "$@"` with the correct venv Python; commits made with `PATH=/tmp/bin:$PATH git commit`
- **Files modified:** None (wrapper in /tmp, not committed)
- **Verification:** `pre-commit --version` returns `pre-commit 4.0.1`; commit succeeded with all hooks passing
- **Committed in:** N/A (runtime fix only)

---

**Total deviations:** 2 auto-fixed (1 formatting/lint, 1 blocking environment issue)
**Impact on plan:** Both fixes necessary for correctness and ability to commit. No scope creep. No behavioral changes to the 3 committed files.

## Issues Encountered

- Broken venv shebang paths throughout `.venv/bin/` (all scripts reference old path without `wcl/`). Worked around for commits using PATH override. Pre-existing environment issue not introduced by this plan.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Three files committed; workflow will trigger on pushes to `roles/sssd_ad_integration/**` when that role exists
- Phase 6 (usbguard) can proceed — no dependency on sssd_ad_integration
- sssd_ad_integration role development can reference this workflow as the test harness

---
*Phase: 05.1-ssh-access-vm-test*
*Completed: 2026-04-20*

## Self-Check: PASSED

- FOUND: `.github/workflows/ssh-access-vm-test.yml`
- FOUND: `playbooks/ssh-access-vm-test.yml`
- FOUND: `roles/user_audit/tests/inventory`
- FOUND: commit `148d1d3` (ci - 3 VM test files)
- FOUND: commit `ae6f70d` (docs - ROADMAP phase 05.1 planning)
- FOUND: commit `e1e6579` (docs - plan complete metadata)
