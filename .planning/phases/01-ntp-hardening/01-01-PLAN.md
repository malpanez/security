---
plan: "01-01: Scaffold ntp_hardening role"
phase: 1
wave: 1
depends_on: []
files_modified:
  - roles/ntp_hardening/defaults/main.yml
  - roles/ntp_hardening/handlers/main.yml
  - roles/ntp_hardening/meta/main.yml
  - roles/ntp_hardening/meta/argument_specs.yml
  - roles/ntp_hardening/tests/test.yml
  - roles/ntp_hardening/vars/Debian.yml
  - roles/ntp_hardening/vars/RedHat.yml
  - roles/ntp_hardening/vars/Suse.yml
requirements_addressed: [STD-01, STD-06, STD-07, STD-08, STD-09, STD-14, NTP-06]
autonomous: true
must_haves:
  truths:
    - "Role directory exists with ansible-galaxy standard structure"
    - "All defaults variables are defined with correct types and values"
    - "OS-family vars provide correct package names, service names, and config paths"
    - "Galaxy metadata passes schema validation"
    - "argument_specs documents every defaults variable"
    - "Compliance tags present in galaxy_tags"
  artifacts:
    - path: "roles/ntp_hardening/defaults/main.yml"
      provides: "All role defaults"
      contains: "ntp_hardening_enabled"
    - path: "roles/ntp_hardening/vars/Debian.yml"
      provides: "Debian-family package/service/config paths"
      contains: "ntp_hardening_service: chrony"
    - path: "roles/ntp_hardening/vars/RedHat.yml"
      provides: "RedHat-family package/service/config paths"
      contains: "ntp_hardening_service: chronyd"
    - path: "roles/ntp_hardening/vars/Suse.yml"
      provides: "Suse-family package/service/config paths"
      contains: "ntp_hardening_service: chronyd"
    - path: "roles/ntp_hardening/meta/main.yml"
      provides: "Galaxy metadata with compliance tags"
      contains: "min_ansible_version"
    - path: "roles/ntp_hardening/meta/argument_specs.yml"
      provides: "Variable documentation for all defaults"
      contains: "ntp_hardening_servers"
    - path: "roles/ntp_hardening/tests/test.yml"
      provides: "Test playbook with FQCN"
      contains: "malpanez.security.ntp_hardening"
    - path: "roles/ntp_hardening/handlers/main.yml"
      provides: "Restart chronyd handler"
      contains: "Restart chronyd"
  key_links:
    - from: "roles/ntp_hardening/defaults/main.yml"
      to: "roles/ntp_hardening/meta/argument_specs.yml"
      via: "every default var documented in argument_specs"
      pattern: "ntp_hardening_"
---

## Objective

Scaffold the ntp_hardening role using `ansible-galaxy role init`, then populate defaults, OS-family vars, meta, argument_specs, handler, and tests. This creates the foundation all subsequent plans build on.

Purpose: Establish role structure and variable contracts before any task logic is written.
Output: Complete role skeleton with all variable definitions, Galaxy metadata, and handler.

## Tasks

<task type="auto">
  <name>Task 1: Scaffold role and populate defaults, vars, handler, tests, meta</name>
  <read_first>
    - roles/aide/defaults/main.yml
    - roles/aide/meta/main.yml
    - roles/aide/meta/argument_specs.yml
    - roles/aide/handlers/main.yml
    - roles/aide/tests/test.yml
    - roles/aide/vars/Debian.yml
    - roles/aide/vars/RedHat.yml
    - .planning/phases/01-ntp-hardening/01-RESEARCH.md
  </read_first>
  <files>
    roles/ntp_hardening/ (scaffold)
    roles/ntp_hardening/defaults/main.yml
    roles/ntp_hardening/vars/Debian.yml
    roles/ntp_hardening/vars/RedHat.yml
    roles/ntp_hardening/vars/Suse.yml
    roles/ntp_hardening/handlers/main.yml
    roles/ntp_hardening/meta/main.yml
    roles/ntp_hardening/meta/argument_specs.yml
    roles/ntp_hardening/tests/test.yml
  </files>
  <action>
1. Run `ansible-galaxy role init roles/ntp_hardening` to create the scaffold (STD-01).

2. Write `defaults/main.yml` with these exact variables and values:
```yaml
---
ntp_hardening_enabled: false

ntp_hardening_servers:
  - 0.pool.ntp.org iburst
  - 1.pool.ntp.org iburst
  - 2.pool.ntp.org iburst
  - 3.pool.ntp.org iburst

ntp_hardening_makestep: "1.0 3"
ntp_hardening_rtcsync: true
ntp_hardening_maxdistance: 1.5
ntp_hardening_minsources: 1
ntp_hardening_allow: []
ntp_hardening_deny_all: true
ntp_hardening_leapsectz: "right/UTC"
ntp_hardening_logdir: /var/log/chrony
ntp_hardening_logchange: 0.5
```

3. Write `vars/Debian.yml`:
```yaml
---
ntp_hardening_package: chrony
ntp_hardening_service: chrony
ntp_hardening_config_file: /etc/chrony/chrony.conf
ntp_hardening_conf_d: /etc/chrony/conf.d
```

4. Write `vars/RedHat.yml`:
```yaml
---
ntp_hardening_package: chrony
ntp_hardening_service: chronyd
ntp_hardening_config_file: /etc/chrony.conf
ntp_hardening_conf_d: /etc/chrony.d
```

5. Write `vars/Suse.yml` (same content as RedHat.yml):
```yaml
---
ntp_hardening_package: chrony
ntp_hardening_service: chronyd
ntp_hardening_config_file: /etc/chrony.conf
ntp_hardening_conf_d: /etc/chrony.d
```

6. Write `handlers/main.yml`:
```yaml
---
- name: Restart chronyd
  ansible.builtin.systemd:
    name: "{{ ntp_hardening_service }}"
    state: restarted
  changed_when: true
  when: not _ntp_in_container | bool
```

7. Write `meta/main.yml` — mirror aide meta exactly for schema:
```yaml
---
galaxy_info:
  author: Miguel Alpanez
  description: >-
    NTP hardening with chrony. Installs chrony, disables competing time daemons,
    deploys a hardened chrony.conf, and enables the service. Supports review mode
    (audit-only) and enforce mode. PCI-DSS 10.6.1, NIS2 Art.21, SOC2 CC6.1.
  license: GPL-2.0-or-later
  min_ansible_version: "2.16"
  galaxy_tags:
    - security
    - hardening
    - ntp
    - chrony
    - timesync
    - linux
    - pci
    - nis2
    - soc2
    - hipaa
    - compliance
  platforms:
    - name: Debian
      versions: [buster, bullseye, bookworm]
    - name: Ubuntu
      versions: [bionic, focal, jammy]
    - name: EL
      versions: [all]
dependencies: []
```

8. Write `meta/argument_specs.yml` documenting every defaults variable. Each entry must have `type` and `description`. Use the aide argument_specs as structural reference. Variables to document:
   - ntp_hardening_enabled (bool, default false)
   - ntp_hardening_servers (list, default pool.ntp.org servers)
   - ntp_hardening_makestep (str, default "1.0 3")
   - ntp_hardening_rtcsync (bool, default true)
   - ntp_hardening_maxdistance (float, default 1.5)
   - ntp_hardening_minsources (int, default 1)
   - ntp_hardening_allow (list, default [])
   - ntp_hardening_deny_all (bool, default true)
   - ntp_hardening_leapsectz (str, default "right/UTC")
   - ntp_hardening_logdir (str, default /var/log/chrony)
   - ntp_hardening_logchange (float, default 0.5)

9. Write `tests/test.yml` (mirror aide exactly):
```yaml
---
- name: Test ntp_hardening role
  hosts: localhost
  gather_facts: true
  roles:
    - role: malpanez.security.ntp_hardening
```

10. Reinstall collection: `ansible-galaxy collection install . --force -p .ansible/collections` (STD-14).

11. Delete auto-generated boilerplate files that are empty/unused: `roles/ntp_hardening/files/.gitkeep` if created, and clear the auto-generated README.md placeholder content (leave file present but minimal).
  </action>
  <verify>
    <automated>
test -d roles/ntp_hardening/defaults && \
test -d roles/ntp_hardening/handlers && \
test -d roles/ntp_hardening/meta && \
test -d roles/ntp_hardening/vars && \
test -f roles/ntp_hardening/tests/test.yml && \
grep -q 'ntp_hardening_enabled' roles/ntp_hardening/defaults/main.yml && \
grep -q 'ntp_hardening_service: chrony$' roles/ntp_hardening/vars/Debian.yml && \
grep -q 'ntp_hardening_service: chronyd' roles/ntp_hardening/vars/RedHat.yml && \
grep -q 'ntp_hardening_service: chronyd' roles/ntp_hardening/vars/Suse.yml && \
grep -q 'changed_when: true' roles/ntp_hardening/handlers/main.yml && \
grep -q 'malpanez.security.ntp_hardening' roles/ntp_hardening/tests/test.yml && \
grep -q 'min_ansible_version' roles/ntp_hardening/meta/main.yml && \
grep -qE 'pci|nis2|soc2|hipaa' roles/ntp_hardening/meta/main.yml && \
grep -q 'ntp_hardening_servers' roles/ntp_hardening/meta/argument_specs.yml && \
yamllint roles/ntp_hardening/defaults/main.yml roles/ntp_hardening/vars/ roles/ntp_hardening/meta/ roles/ntp_hardening/handlers/main.yml roles/ntp_hardening/tests/test.yml && \
echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `roles/ntp_hardening/` directory exists with defaults/, handlers/, meta/, vars/, tests/, tasks/, templates/ subdirs
    - `defaults/main.yml` contains exactly 11 variables: ntp_hardening_enabled, ntp_hardening_servers, ntp_hardening_makestep, ntp_hardening_rtcsync, ntp_hardening_maxdistance, ntp_hardening_minsources, ntp_hardening_allow, ntp_hardening_deny_all, ntp_hardening_leapsectz, ntp_hardening_logdir, ntp_hardening_logchange
    - `vars/Debian.yml` has ntp_hardening_service: chrony, ntp_hardening_config_file: /etc/chrony/chrony.conf
    - `vars/RedHat.yml` has ntp_hardening_service: chronyd, ntp_hardening_config_file: /etc/chrony.conf
    - `vars/Suse.yml` has ntp_hardening_service: chronyd, ntp_hardening_config_file: /etc/chrony.conf
    - `handlers/main.yml` contains `changed_when: true` (not false) and `when: not _ntp_in_container | bool`
    - `meta/main.yml` has platforms with Debian [buster, bullseye, bookworm], Ubuntu [bionic, focal, jammy], EL [all]
    - `meta/main.yml` galaxy_tags include: pci, nis2, soc2, hipaa, ntp, chrony
    - `meta/argument_specs.yml` has an entry for every variable in defaults/main.yml
    - `tests/test.yml` has play name "Test ntp_hardening role" and role: malpanez.security.ntp_hardening
    - `yamllint` passes on all created YAML files
    - Collection reinstalled successfully via ansible-galaxy
  </acceptance_criteria>
  <done>Role scaffold complete with all defaults, OS vars, meta, argument_specs, handler, and tests. Collection reinstalled.</done>
</task>

## Verification

```bash
test -d roles/ntp_hardening && \
yamllint roles/ntp_hardening/defaults/ roles/ntp_hardening/vars/ roles/ntp_hardening/meta/ roles/ntp_hardening/handlers/ roles/ntp_hardening/tests/ && \
echo "Plan 01-01 verified"
```

## must_haves

```yaml
truths:
  - "Role directory created via ansible-galaxy role init"
  - "All 11 defaults variables defined with correct types"
  - "Three OS-family vars files provide package, service, config_file, conf_d"
  - "Galaxy metadata passes schema (correct platform versions)"
  - "Compliance tags present in galaxy_tags"
  - "Handler uses changed_when: true and container guard"
  - "tests/test.yml uses FQCN malpanez.security.ntp_hardening"
  - "argument_specs covers all defaults variables"
artifacts:
  - path: "roles/ntp_hardening/defaults/main.yml"
    contains: "ntp_hardening_enabled: false"
  - path: "roles/ntp_hardening/vars/Debian.yml"
    contains: "ntp_hardening_service: chrony"
  - path: "roles/ntp_hardening/vars/RedHat.yml"
    contains: "ntp_hardening_service: chronyd"
  - path: "roles/ntp_hardening/meta/main.yml"
    contains: "versions: [all]"
  - path: "roles/ntp_hardening/meta/argument_specs.yml"
    contains: "ntp_hardening_servers"
  - path: "roles/ntp_hardening/tests/test.yml"
    contains: "malpanez.security.ntp_hardening"
  - path: "roles/ntp_hardening/handlers/main.yml"
    contains: "changed_when: true"
key_links:
  - from: "defaults/main.yml"
    to: "meta/argument_specs.yml"
    via: "1:1 variable documentation"
    pattern: "ntp_hardening_"
```
