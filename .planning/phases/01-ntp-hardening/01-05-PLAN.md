---
plan: "01-05: CI integration — add ntp_hardening to workflow matrices"
phase: 1
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - .github/workflows/ci-uv.yml
  - .github/workflows/ci-cd-enterprise.yml
requirements_addressed: [STD-13]
autonomous: true
must_haves:
  truths:
    - "ntp_hardening appears in ci-uv.yml test-roles matrix"
    - "ntp_hardening appears in ci-cd-enterprise.yml test-molecule matrix"
  artifacts:
    - path: ".github/workflows/ci-uv.yml"
      provides: "CI test matrix including ntp_hardening"
      contains: "ntp_hardening"
    - path: ".github/workflows/ci-cd-enterprise.yml"
      provides: "Enterprise CI test matrix including ntp_hardening"
      contains: "ntp_hardening"
  key_links:
    - from: ".github/workflows/ci-uv.yml"
      to: "roles/ntp_hardening/molecule/default/"
      via: "matrix.role -> cd roles/$role && molecule test"
      pattern: "ntp_hardening"
---

## Objective

Add ntp_hardening to both CI workflow test matrices so the role is automatically tested on push/PR.

Purpose: Ensure CI runs Molecule tests for ntp_hardening alongside existing roles.
Output: Updated ci-uv.yml and ci-cd-enterprise.yml with ntp_hardening in their matrices.

## Tasks

<task type="auto">
  <name>Task 1: Add ntp_hardening to CI workflow matrices</name>
  <read_first>
    - .github/workflows/ci-uv.yml
    - .github/workflows/ci-cd-enterprise.yml
  </read_first>
  <files>
    .github/workflows/ci-uv.yml
    .github/workflows/ci-cd-enterprise.yml
  </files>
  <action>
1. In `.github/workflows/ci-uv.yml`, find the `test-roles` job matrix (around line 125). Add `- ntp_hardening` to the `role:` list. Place it alphabetically (after `grype_scanner`, before `security_capabilities`). The current list is:
```yaml
        role:
          - security_capabilities
          - sudoers_baseline
          - selinux_enforcement
          - service_accounts_transfer
          - compliance_evidence
          - cis_baseline
          - grype_scanner
          - aide
```
Add `- ntp_hardening` to this list. The exact position does not matter but alphabetical is preferred.

2. In `.github/workflows/ci-cd-enterprise.yml`, find the `test-molecule` job matrix (around line 325). Add `- ntp_hardening` to the `role:` list. The current list is:
```yaml
        role:
          - security_capabilities
          - sshd_hardening
          - pam_mfa
          - sudoers_baseline
          - selinux_enforcement
          - service_accounts_transfer
          - audit_logging
          - compliance_evidence
          - cis_baseline
          - grype_scanner
          - aide
```
Add `- ntp_hardening` to this list.

3. Do NOT modify any other part of these workflow files. Do not change triggers, checkout actions, UV versions, or any other matrix entries.

4. Run yamllint on both workflow files to confirm no syntax errors were introduced.
  </action>
  <verify>
    <automated>
grep -q 'ntp_hardening' .github/workflows/ci-uv.yml && \
grep -q 'ntp_hardening' .github/workflows/ci-cd-enterprise.yml && \
yamllint .github/workflows/ci-uv.yml && \
yamllint .github/workflows/ci-cd-enterprise.yml && \
echo "PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'ntp_hardening' .github/workflows/ci-uv.yml` returns rc=0
    - `grep -q 'ntp_hardening' .github/workflows/ci-cd-enterprise.yml` returns rc=0
    - `yamllint .github/workflows/ci-uv.yml` passes with 0 errors
    - `yamllint .github/workflows/ci-cd-enterprise.yml` passes with 0 errors
    - No other changes made to the workflow files (only the matrix entry added)
  </acceptance_criteria>
  <done>ntp_hardening added to both CI workflow test matrices.</done>
</task>

## Verification

```bash
grep 'ntp_hardening' .github/workflows/ci-uv.yml .github/workflows/ci-cd-enterprise.yml && \
echo "Plan 01-05 verified"
```

## must_haves

```yaml
truths:
  - "ntp_hardening in ci-uv.yml test-roles matrix"
  - "ntp_hardening in ci-cd-enterprise.yml test-molecule matrix"
artifacts:
  - path: ".github/workflows/ci-uv.yml"
    contains: "ntp_hardening"
  - path: ".github/workflows/ci-cd-enterprise.yml"
    contains: "ntp_hardening"
key_links:
  - from: ".github/workflows/ci-uv.yml"
    to: "roles/ntp_hardening/"
    via: "matrix.role cd + molecule test"
    pattern: "ntp_hardening"
```
