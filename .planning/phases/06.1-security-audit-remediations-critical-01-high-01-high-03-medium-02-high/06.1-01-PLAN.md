---
phase: 06.1-security-audit-remediations-critical-01-high-01-high-03-medium-02-high
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/sssd_ad_integration/defaults/main.yml
  - roles/sssd_ad_integration/meta/argument_specs.yml
  - roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2
autonomous: true
requirements: [AUDIT-CRIT-01, AUDIT-HIGH-01, AUDIT-HIGH-03]

must_haves:
  truths:
    - "Corp LAN admins get MFA when ssh_corp_require_mfa is true (AuthenticationMethods publickey,keyboard-interactive in Match Address corp block)"
    - "Corp LAN admins get single-factor when ssh_corp_require_mfa is false (backward compat preserved)"
    - "Offline credential cache expires after 7 days by default (sssd_offline_credentials_expiration: 7)"
    - "Group verification runs in strict mode by default (ssh_verify_groups_strict: true), failing the play on unresolvable groups"
  artifacts:
    - path: "roles/sssd_ad_integration/defaults/main.yml"
      provides: "Updated defaults for all 3 findings"
      contains: "ssh_corp_require_mfa"
    - path: "roles/sssd_ad_integration/defaults/main.yml"
      provides: "Offline cred expiration changed"
      contains: "sssd_offline_credentials_expiration: 7"
    - path: "roles/sssd_ad_integration/defaults/main.yml"
      provides: "Strict verify default changed"
      contains: "ssh_verify_groups_strict: true"
    - path: "roles/sssd_ad_integration/meta/argument_specs.yml"
      provides: "Documentation of new variable and updated descriptions"
      contains: "ssh_corp_require_mfa"
    - path: "roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2"
      provides: "Conditional MFA in corp Match Address block"
      contains: "ssh_corp_require_mfa"
  key_links:
    - from: "roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2"
      to: "roles/sssd_ad_integration/defaults/main.yml"
      via: "ssh_corp_require_mfa variable"
      pattern: "ssh_corp_require_mfa"
---

<objective>
Remediate 3 audit findings in the sssd_ad_integration role: AUDIT-CRIT-01 (corp LAN MFA downgrade), AUDIT-HIGH-01 (offline cred expiration never expires), AUDIT-HIGH-03 (verify_groups_strict defaults to unsafe false).

Purpose: Close critical and high-severity SSH security audit findings without breaking existing deployments.
Output: Updated defaults, argument_specs, and 30-network-zones template with secure-by-default values and a new MFA toggle variable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@roles/sssd_ad_integration/defaults/main.yml
@roles/sssd_ad_integration/meta/argument_specs.yml
@roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2
@roles/sssd_ad_integration/templates/sshd/40-group-admins.conf.j2
@roles/sssd_ad_integration/tasks/verify_groups.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sssd_ad_integration defaults and argument_specs for CRIT-01, HIGH-01, HIGH-03</name>
  <files>roles/sssd_ad_integration/defaults/main.yml, roles/sssd_ad_integration/meta/argument_specs.yml</files>
  <read_first>roles/sssd_ad_integration/defaults/main.yml, roles/sssd_ad_integration/meta/argument_specs.yml</read_first>
  <action>
In roles/sssd_ad_integration/defaults/main.yml:

1. AUDIT-CRIT-01: Add new variable `ssh_corp_require_mfa: false` in the network zones section (after the `ssh_network_zones` dict, before the sshd drop-in directory section). Add inline comment:
   ```
   # Require MFA (publickey + keyboard-interactive) even from corporate LAN.
   # Default false preserves backward compatibility. Set true to close AUDIT-CRIT-01:
   # without this, Match Address corp (file 30) sets AuthenticationMethods publickey,
   # which wins over Match Group linux-admins (file 40) publickey,keyboard-interactive
   # due to sshd first-occurrence-wins rule.
   ssh_corp_require_mfa: false
   ```

2. AUDIT-HIGH-01: Change `sssd_offline_credentials_expiration: 0` to `sssd_offline_credentials_expiration: 7`. Update the inline comment from `# 0=never. Document this decision explicitly.` to:
   ```
   # Days cached credentials remain valid when AD is unreachable.
   # 0 = no expiry (AUDIT-HIGH-01 risk: disabled AD accounts authenticate indefinitely).
   # 7 = credentials expire after 7 days offline. Adjust per site availability needs.
   sssd_offline_credentials_expiration: 7
   ```

3. AUDIT-HIGH-03: Change `ssh_verify_groups_strict: false` to `ssh_verify_groups_strict: true`. Update the comments above this block:
   ```
   # ssh_verify_groups_strict:
   #   true  (default) — fail the play if any group is missing or has no members.
   #           Prevents silent lockout from unresolvable AllowGroups entries.
   #           OpenSSH deny-by-default: unresolvable group = ALL users denied.
   #   false — warn via debug, continue play. Use only during initial rollout
   #           when groups are not yet populated. Set ssh_local_fallback_group
   #           to prevent total lockout.
   ```

In roles/sssd_ad_integration/meta/argument_specs.yml:

4. Add `ssh_corp_require_mfa` option after the `ssh_network_zones` section:
   ```yaml
      ssh_corp_require_mfa:
        type: bool
        default: false
        description:
          - Require 2FA (publickey + keyboard-interactive) for the corp LAN zone.
          - 'When false (default): corp LAN uses publickey only (backward compatible).'
          - 'When true: corp LAN uses publickey,keyboard-interactive, closing the'
          - 'sshd first-occurrence-wins loophole where file 30 (Match Address) overrides'
          - 'file 40 (Match Group linux-admins) AuthenticationMethods.'
          - 'Risk acceptance: set false only if corp LAN is trusted and MFA adds no value.'
   ```

5. Update `sssd_offline_credentials_expiration` description — change default from `0` to `7` and update the description lines:
   ```yaml
      sssd_offline_credentials_expiration:
        type: int
        default: 7
        description:
          - Days to accept cached credentials when AD is unreachable.
          - '7 days balances offline access against credential staleness.'
          - '0 means never expire — disabled AD accounts can authenticate indefinitely.'
          - Set higher for sites with unreliable WAN. Set 0 only with documented risk acceptance.
   ```

6. Update `ssh_verify_ad_groups_strict` description — change default from `false` to `true` and update description:
   ```yaml
      ssh_verify_ad_groups_strict:
        type: bool
        default: true
        description:
          - 'true (default): fail the play if any enabled group is unresolvable or empty.'
          - 'Prevents deploying AllowGroups with groups OpenSSH cannot resolve.'
          - 'OpenSSH deny-by-default: unresolvable group means ALL users are denied.'
          - 'false: warn and continue. Use only during initial rollout.'
          - 'Mitigate lockout risk with ssh_local_fallback_group.'
   ```
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/wcl/malpanez/security && grep -q 'ssh_corp_require_mfa: false' roles/sssd_ad_integration/defaults/main.yml && grep -q 'sssd_offline_credentials_expiration: 7' roles/sssd_ad_integration/defaults/main.yml && grep -q 'ssh_verify_groups_strict: true' roles/sssd_ad_integration/defaults/main.yml && grep -q 'ssh_corp_require_mfa' roles/sssd_ad_integration/meta/argument_specs.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'ssh_corp_require_mfa: false' roles/sssd_ad_integration/defaults/main.yml returns match
    - grep 'sssd_offline_credentials_expiration: 7' roles/sssd_ad_integration/defaults/main.yml returns match
    - grep 'ssh_verify_groups_strict: true' roles/sssd_ad_integration/defaults/main.yml returns match
    - grep 'ssh_corp_require_mfa' roles/sssd_ad_integration/meta/argument_specs.yml returns match
    - grep 'default: 7' roles/sssd_ad_integration/meta/argument_specs.yml returns match for offline_credentials
    - grep 'default: true' roles/sssd_ad_integration/meta/argument_specs.yml returns match for verify_ad_groups_strict
    - yamllint roles/sssd_ad_integration/defaults/main.yml exits 0
    - yamllint roles/sssd_ad_integration/meta/argument_specs.yml exits 0
  </acceptance_criteria>
  <done>defaults/main.yml has ssh_corp_require_mfa:false, sssd_offline_credentials_expiration:7, ssh_verify_groups_strict:true. argument_specs.yml documents all 3 changes with risk descriptions. yamllint passes on both files.</done>
</task>

<task type="auto">
  <name>Task 2: Update 30-network-zones.conf.j2 to conditionally require MFA on corp LAN</name>
  <files>roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2</files>
  <read_first>roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2</read_first>
  <action>
In roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2:

Replace the corp Match Address block's `AuthenticationMethods publickey` line (line 27) with a Jinja2 conditional:

```jinja2
{% if ssh_corp_require_mfa | default(false) | bool %}
    AuthenticationMethods publickey,keyboard-interactive
{% else %}
    AuthenticationMethods publickey
{% endif %}
```

Also update the file header comment block. Replace lines 13-17 (the "Example:" paragraph) with:

```
#   Example: admin from corp LAN triggers Match Address corp AND
#   Match Group linux-admins.  When ssh_corp_require_mfa=false (default),
#   the corp block sets AuthenticationMethods publickey (single factor) —
#   this wins over linux-admins's publickey,keyboard-interactive (2FA).
#   Set ssh_corp_require_mfa=true to require 2FA even from corp LAN.
```

Do NOT change the vpn or bastion blocks — those remain publickey only (no MFA toggle needed for non-interactive zones).
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/wcl/malpanez/security && grep -q 'ssh_corp_require_mfa' roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2 && grep -q 'publickey,keyboard-interactive' roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2 && ansible-lint roles/sssd_ad_integration/ 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'ssh_corp_require_mfa' roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2 returns match
    - grep 'publickey,keyboard-interactive' roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2 returns match (the MFA auth method)
    - grep 'publickey$' roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2 returns match (the non-MFA fallback still present in else branch)
    - The vpn and bastion blocks remain unchanged (still AuthenticationMethods publickey, no conditional)
    - yamllint on all changed YAML files exits 0
    - ansible-lint roles/sssd_ad_integration/ reports no errors on the template
  </acceptance_criteria>
  <done>30-network-zones.conf.j2 corp block conditionally emits publickey,keyboard-interactive when ssh_corp_require_mfa is true, publickey when false. VPN and bastion blocks unchanged. ansible-lint passes.</done>
</task>

</tasks>

<verification>
All 3 sssd_ad_integration audit findings remediated:
- AUDIT-CRIT-01: ssh_corp_require_mfa variable controls MFA in corp Match Address block
- AUDIT-HIGH-01: sssd_offline_credentials_expiration defaults to 7 (was 0)
- AUDIT-HIGH-03: ssh_verify_groups_strict defaults to true (was false)

Run: `yamllint roles/sssd_ad_integration/defaults/main.yml roles/sssd_ad_integration/meta/argument_specs.yml`
Run: `ansible-lint roles/sssd_ad_integration/`
</verification>

<success_criteria>
1. ssh_corp_require_mfa: false exists in defaults, documented in argument_specs, and wired into 30-network-zones.conf.j2 conditional
2. sssd_offline_credentials_expiration: 7 in defaults, argument_specs default updated
3. ssh_verify_groups_strict: true in defaults, argument_specs default and description updated
4. yamllint + ansible-lint pass with 0 errors on all modified files
5. Backward compatibility preserved: all 3 changes use safe defaults or are opt-in
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-security-audit-remediations-critical-01-high-01-high-03-medium-02-high/06.1-01-SUMMARY.md`
</output>
