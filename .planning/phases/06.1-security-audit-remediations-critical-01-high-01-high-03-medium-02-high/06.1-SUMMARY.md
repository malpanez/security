---
phase: 06.1-security-audit-remediations-critical-01-high-01-high-03-medium-02-high
status: complete
completed: 2026-04-20
plans_completed: 2
findings_closed: 4
---

# Phase 06.1 Summary — Security Audit Remediations

## Findings Closed

| ID | Severity | Role | Change |
|----|----------|------|--------|
| AUDIT-CRIT-01 | Critical | sssd_ad_integration | Added `ssh_corp_require_mfa` (default false); wired into 30-network-zones.conf.j2 conditional — operators set true to require MFA on corp LAN |
| AUDIT-HIGH-01 | High | sssd_ad_integration | `sssd_offline_credentials_expiration` 0 → 7 (was unlimited; disabled AD accounts could authenticate indefinitely offline) |
| AUDIT-HIGH-03 | High | sssd_ad_integration | `ssh_verify_groups_strict` false → true (was silent — unresolvable AllowGroups group = all users denied) |
| AUDIT-MED-02 | High | antivirus | `clamav-scan.service.j2` User=root → `{{ antivirus_clamd_user }}:{{ antivirus_clamd_group }}` (mitigates CVE-2023-20032 blast radius) |

## Design Decisions

- **AUDIT-CRIT-01 default**: `ssh_corp_require_mfa: false` is intentional backward-compatibility. Existing deployments are not broken. Operators set true to enforce MFA. Finding is closed by providing the opt-in mechanism.
- **Backward compatibility**: All 3 sssd_ad_integration default changes use safe values; operators on old defaults set them explicitly per the README upgrade notes.

## Files Modified

**06.1-01 (sssd_ad_integration):**
- `roles/sssd_ad_integration/defaults/main.yml` — 3 changes
- `roles/sssd_ad_integration/meta/argument_specs.yml` — added ssh_corp_require_mfa, updated defaults and descriptions, added canonical ssh_verify_groups* entries
- `roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2` — Jinja2 conditional on corp MFA
- `roles/sssd_ad_integration/README.md` — upgrade notes for all 3 changed defaults

**06.1-02 (antivirus):**
- `roles/antivirus/templates/clamav-scan.service.j2` — User/Group changed to per-OS vars
- `roles/antivirus/README.md` — documented privilege drop (placeholder → role docs)

## Commits

- `8559612` — fix(sssd_ad_integration): apply AUDIT-CRIT-01/HIGH-01/HIGH-03 remediations
- `029a28f` — fix(sssd_ad_integration): wire ssh_corp_require_mfa into 30-network-zones template
- `bd754f4` — docs(sssd_ad_integration): document 3 changed defaults and upgrade notes
- `d71f621` — fix(antivirus): drop root privilege in clamav-scan.service (AUDIT-MED-02)
- `2bf9743` — docs(antivirus): document privilege drop and upgrade notes
- `b90785d` — fix(sssd_ad_integration): add canonical ssh_verify_groups vars to argument_specs
