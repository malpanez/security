---
phase: 06.1-security-audit-remediations-critical-01-high-01-high-03-medium-02-high
verified: 2026-04-20T17:00:00Z
status: gaps_found
score: 5/6 must-haves verified
gaps:
  - truth: "README.md documents all 3 sssd_ad_integration default changes AND antivirus README documents clamav-scan privilege drop"
    status: partial
    reason: "sssd_ad_integration README is complete. antivirus README was never updated — still contains the ansible-galaxy scaffold placeholder with no mention of antivirus_clamd_user, antivirus_clamd_group, or the AUDIT-MED-02 remediation."
    artifacts:
      - path: "roles/antivirus/README.md"
        issue: "Still contains ansible-galaxy default scaffold text. No variables table, no AUDIT-MED-02 mention, no description of User/Group drop in clamav-scan.service."
    missing:
      - "Replace roles/antivirus/README.md placeholder with actual role documentation covering: antivirus_clamd_user/antivirus_clamd_group variables, clamav-scan.service privilege drop (AUDIT-MED-02), and upgrade notes."
  - truth: "argument_specs.yml documents ssh_corp_require_mfa AND canonical ssh_verify_groups_strict variable"
    status: partial
    reason: "argument_specs.yml documents ssh_corp_require_mfa correctly. However ssh_verify_groups_strict is not documented under its canonical name — only the deprecated aliases ssh_verify_ad_groups and ssh_verify_ad_groups_strict appear in the spec."
    artifacts:
      - path: "roles/sssd_ad_integration/meta/argument_specs.yml"
        issue: "ssh_verify_groups and ssh_verify_groups_strict are not present as spec entries. Only deprecated aliases ssh_verify_ad_groups and ssh_verify_ad_groups_strict are documented (lines 235, 244)."
    missing:
      - "Add ssh_verify_groups (bool, default true) and ssh_verify_groups_strict (bool, default true) entries to argument_specs.yml main options, matching the defaults/main.yml canonical variable names."
---

# Phase 06.1: Security Audit Remediations Verification Report

**Phase Goal:** Remediate 4 confirmed security audit findings across sssd_ad_integration and antivirus roles without breaking existing deployments.
**Verified:** 2026-04-20T17:00:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | AUDIT-CRIT-01: `ssh_corp_require_mfa` exists in defaults (false), documented in argument_specs.yml, wired into 30-network-zones.conf.j2 as MFA conditional | VERIFIED | defaults/main.yml:51, argument_specs.yml:65-74, template:27-31 |
| 2 | AUDIT-HIGH-01: `sssd_offline_credentials_expiration: 7` in defaults | VERIFIED | defaults/main.yml:131 |
| 3 | AUDIT-HIGH-03: `ssh_verify_groups_strict: true` in defaults | VERIFIED | defaults/main.yml:167 |
| 4 | AUDIT-MED-02: clamav-scan.service.j2 runs as `{{ antivirus_clamd_user }}:{{ antivirus_clamd_group }}` | VERIFIED | templates/clamav-scan.service.j2:10-11, vars/RedHat.yml:10-11 |
| 5 | All changed YAML files pass yamllint (0 errors) | VERIFIED | yamllint with .yamllint.yml: EXIT 0, warnings only (line-length) |
| 6 | README.md documents all default changes | PARTIAL FAIL | sssd_ad_integration README complete; antivirus README is ansible-galaxy placeholder |

**Score:** 5/6 truths verified (Truth 6 partial — antivirus README missing)

**Bonus gap found:** argument_specs.yml documents only deprecated aliases for verify_groups, not canonical variable names.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `roles/sssd_ad_integration/defaults/main.yml` | ssh_corp_require_mfa, sssd_offline_credentials_expiration:7, ssh_verify_groups_strict:true | VERIFIED | All three present. Lines 51, 131, 167. |
| `roles/sssd_ad_integration/meta/argument_specs.yml` | ssh_corp_require_mfa documented; ssh_verify_groups_strict documented | PARTIAL | ssh_corp_require_mfa: lines 65-74. ssh_verify_groups_strict: absent — only deprecated alias ssh_verify_ad_groups_strict at line 244. |
| `roles/sssd_ad_integration/templates/sshd/30-network-zones.conf.j2` | Jinja2 conditional emitting publickey,keyboard-interactive when ssh_corp_require_mfa true | VERIFIED | Lines 27-31 implement the conditional correctly. |
| `roles/sssd_ad_integration/README.md` | Documents ssh_corp_require_mfa, sssd_offline_credentials_expiration change, ssh_verify_groups_strict change | VERIFIED | Variables table line 79-83, Upgrade Notes lines 95-101. |
| `roles/antivirus/templates/clamav-scan.service.j2` | User={{ antivirus_clamd_user }} Group={{ antivirus_clamd_group }} | VERIFIED | Lines 10-11. Commit d71f621 confirms change from root. |
| `roles/antivirus/README.md` | Documents antivirus_clamd_user/group variables and AUDIT-MED-02 privilege drop | STUB/MISSING | File contains ansible-galaxy scaffold only. Zero project-specific content. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ssh_corp_require_mfa` default | `30-network-zones.conf.j2` template | Jinja2 conditional `{% if ssh_corp_require_mfa \| default(false) \| bool %}` | WIRED | Template line 27. Emits correct AuthenticationMethods for both states. |
| `antivirus_clamd_user` / `antivirus_clamd_group` | `clamav-scan.service.j2` | `User={{ antivirus_clamd_user }}` / `Group={{ antivirus_clamd_group }}` | WIRED | Template lines 10-11. Vars defined in RedHat.yml, Debian.yml, Suse.yml. |
| `sssd_offline_credentials_expiration` | sssd.conf template | Jinja2 template variable reference | NOT VERIFIED | Verification of sssd.conf.j2 template wiring was out of scope for this phase; value set correctly in defaults. |

---

### Data-Flow Trace (Level 4)

Not applicable. Phase produces Jinja2 templates and role defaults, not dynamic rendering components.

---

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| `ssh_corp_require_mfa: true` emits `publickey,keyboard-interactive` | grep pattern in template | `{% if ssh_corp_require_mfa \| default(false) \| bool %}` → `AuthenticationMethods publickey,keyboard-interactive` found at lines 27-28 | PASS |
| `ssh_corp_require_mfa: false` emits `AuthenticationMethods publickey` | grep pattern in template | `{% else %}` branch → `AuthenticationMethods publickey` found at lines 29-30 | PASS |
| `antivirus_clamd_user` variable is defined for all supported distro families | grep in vars/ | RedHat.yml:10, Debian.yml:10, Suse.yml:8 all define `antivirus_clamd_user` | PASS |
| yamllint on changed YAML | python3 -c yamllint with .yamllint.yml | EXIT 0, warnings only (line-length within 120-char limit) | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| AUDIT-CRIT-01 | 06.1-01 | MFA bypass via corp LAN zone sshd first-occurrence-wins loophole | SATISFIED | ssh_corp_require_mfa variable + template conditional implemented and documented |
| AUDIT-HIGH-01 | 06.1-01 | Offline credentials never expire (value 0) | SATISFIED | sssd_offline_credentials_expiration changed from 0 to 7 in defaults |
| AUDIT-HIGH-03 | 06.1-01 | verify_groups_strict was false — AllowGroups resolution failures silently lock out all users | SATISFIED | ssh_verify_groups_strict: true in defaults; README upgrade notes present |
| AUDIT-MED-02 | 06.1-02 | clamav-scan.service running as root | SATISFIED | clamav-scan.service.j2 now uses per-distro non-root user/group variables |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `roles/antivirus/README.md` | 1-39 | ansible-galaxy default scaffold — no actual content | Blocker | Operators cannot determine what variables to set, what changed in AUDIT-MED-02, or how to configure the role |
| `roles/sssd_ad_integration/meta/argument_specs.yml` | 235, 244 | Canonical variables `ssh_verify_groups` and `ssh_verify_groups_strict` absent from spec; only deprecated aliases documented | Warning | ansible-lint argument_specs validation will not enforce type/default for canonical names; documentation misleads callers |

---

### Human Verification Required

None — all observable truths are verifiable programmatically for this phase.

---

### Gaps Summary

Two gaps block full goal achievement:

**Gap 1 — antivirus README is a placeholder (Blocker).**
`roles/antivirus/README.md` was never updated from the ansible-galaxy scaffold. The file contains generic boilerplate with no reference to `antivirus_clamd_user`, `antivirus_clamd_group`, the AUDIT-MED-02 remediation, or any role-specific content. Commit `d71f621` only touched the service template. The README must be replaced with actual role documentation.

**Gap 2 — argument_specs.yml missing canonical verify_groups variable entries (Warning).**
`defaults/main.yml` defines `ssh_verify_groups` (line 166) and `ssh_verify_groups_strict` (line 167) as the canonical names, with deprecated aliases pointing to them via Jinja2 references (lines 170-171). However `meta/argument_specs.yml` only documents the deprecated aliases `ssh_verify_ad_groups` and `ssh_verify_ad_groups_strict`. The canonical names are absent from the spec, meaning ansible-lint argument_specs enforcement will not validate their type or default, and callers reading the spec will not find the current variable name.

---

_Verified: 2026-04-20T17:00:00Z_
_Verifier: Claude (gsd-verifier)_
