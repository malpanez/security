---
phase: 06.1-security-audit-remediations-critical-01-high-01-high-03-medium-02-high
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/antivirus/templates/clamav-scan.service.j2
autonomous: true
requirements: [AUDIT-MED-02]

must_haves:
  truths:
    - "clamav-scan.service runs as the clamav user, not root (User={{ antivirus_clamd_user }})"
    - "Scan targets /home, /tmp, /var/tmp which are world-readable — no permission escalation needed"
    - "Scan log directory is already owned by antivirus_clamd_user (per enforce.yml line 44-49)"
  artifacts:
    - path: "roles/antivirus/templates/clamav-scan.service.j2"
      provides: "Non-root scan service"
      contains: "antivirus_clamd_user"
  key_links:
    - from: "roles/antivirus/templates/clamav-scan.service.j2"
      to: "roles/antivirus/vars/Debian.yml"
      via: "antivirus_clamd_user variable (clamav on Debian, clamscan on RedHat)"
      pattern: "antivirus_clamd_user"
    - from: "roles/antivirus/templates/clamav-scan.service.j2"
      to: "roles/antivirus/tasks/enforce.yml"
      via: "Log dir ownership already set to antivirus_clamd_user (enforce.yml lines 34-49)"
      pattern: "antivirus_clamd_user"
---

<objective>
Remediate AUDIT-MED-02: ClamAV scan service runs as root. Change User=root to User={{ antivirus_clamd_user }} in the systemd service template.

Purpose: Eliminate root-privilege execution of clamscan, mitigating CVE-2023-20032 (CVSS 9.8 RCE) impact. The clamav user already owns the log directory and scan targets are world-readable.
Output: Updated clamav-scan.service.j2 template running as non-root user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@roles/antivirus/templates/clamav-scan.service.j2
@roles/antivirus/defaults/main.yml
@roles/antivirus/tasks/enforce.yml
@roles/antivirus/vars/Debian.yml
@roles/antivirus/vars/RedHat.yml

<interfaces>
From roles/antivirus/vars/Debian.yml:
  antivirus_clamd_user: clamav

From roles/antivirus/vars/RedHat.yml:
  antivirus_clamd_user: clamscan

From roles/antivirus/tasks/enforce.yml (lines 34-49):
  Log dir and scan log dir are already owned by antivirus_clamd_user:antivirus_clamd_group with mode 0750.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change clamav-scan.service.j2 to run as non-root user</name>
  <files>roles/antivirus/templates/clamav-scan.service.j2</files>
  <read_first>roles/antivirus/templates/clamav-scan.service.j2, roles/antivirus/vars/Debian.yml, roles/antivirus/vars/RedHat.yml</read_first>
  <action>
In roles/antivirus/templates/clamav-scan.service.j2:

Replace line 10 `User=root` with `User={{ antivirus_clamd_user }}`.

Also add `Group={{ antivirus_clamd_group }}` on the line immediately after the User line. This ensures the process runs with the correct group for log file writes.

The final [Service] section should read:
```
[Service]
Type=oneshot
User={{ antivirus_clamd_user }}
Group={{ antivirus_clamd_group }}
ExecStart=/usr/bin/clamscan \
```

No other changes to the template. The scan targets (/home, /tmp, /var/tmp) are world-readable by default. The scan log directory is already owned by antivirus_clamd_user per enforce.yml (Create scan log directory task, lines 44-49).

Do NOT add any CapabilityBoundingSet, ProtectHome, or other hardening directives — those would break the scan functionality. The security improvement is purely the privilege drop from root to clamav user.
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/wcl/malpanez/security && grep -q 'User={{ antivirus_clamd_user }}' roles/antivirus/templates/clamav-scan.service.j2 && grep -q 'Group={{ antivirus_clamd_group }}' roles/antivirus/templates/clamav-scan.service.j2 && ! grep -q 'User=root' roles/antivirus/templates/clamav-scan.service.j2 && ansible-lint roles/antivirus/ 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'User={{ antivirus_clamd_user }}' roles/antivirus/templates/clamav-scan.service.j2 returns match
    - grep 'Group={{ antivirus_clamd_group }}' roles/antivirus/templates/clamav-scan.service.j2 returns match
    - grep 'User=root' roles/antivirus/templates/clamav-scan.service.j2 returns NO match (root removed)
    - ansible-lint roles/antivirus/ reports no errors
    - yamllint on any changed YAML files exits 0 (no YAML changes expected in this task, but verify if any were touched)
  </acceptance_criteria>
  <done>clamav-scan.service.j2 runs as antivirus_clamd_user:antivirus_clamd_group instead of root. No User=root remains. ansible-lint passes.</done>
</task>

</tasks>

<verification>
AUDIT-MED-02 remediated:
- clamav-scan.service runs as antivirus_clamd_user (clamav on Debian, clamscan on RedHat)
- No root execution of clamscan binary
- Log directory ownership already correct (enforce.yml handles this)

Run: `ansible-lint roles/antivirus/`
Run: `grep -n 'User=' roles/antivirus/templates/clamav-scan.service.j2` (must show antivirus_clamd_user, not root)
</verification>

<success_criteria>
1. clamav-scan.service.j2 contains User={{ antivirus_clamd_user }} and Group={{ antivirus_clamd_group }}
2. No occurrence of User=root in the template
3. ansible-lint passes with 0 errors on the antivirus role
4. Template still renders valid systemd unit syntax (Type=oneshot, ExecStart intact)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-security-audit-remediations-critical-01-high-01-high-03-medium-02-high/06.1-02-SUMMARY.md`
</output>
