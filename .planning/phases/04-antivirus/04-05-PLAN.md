---
phase: 04-antivirus
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - .github/workflows/ci-uv.yml
  - .github/workflows/ci-cd-enterprise.yml
autonomous: true
requirements: [STD-12, STD-13, STD-14]

must_haves:
  truths:
    - "antivirus appears in ci-uv.yml test-roles matrix"
    - "antivirus appears in ci-cd-enterprise.yml test-molecule matrix"
    - "yamllint and ansible-lint pass with 0 errors on the entire role"
    - "Collection is reinstalled after adding the new role"
  artifacts:
    - path: ".github/workflows/ci-uv.yml"
      provides: "CI test matrix includes antivirus"
      contains: "antivirus"
    - path: ".github/workflows/ci-cd-enterprise.yml"
      provides: "Enterprise CI matrix includes antivirus"
      contains: "antivirus"
  key_links:
    - from: ".github/workflows/ci-uv.yml"
      to: "roles/antivirus/"
      via: "matrix role entry"
      pattern: "- antivirus"
---

<objective>
Add the antivirus role to both CI workflow matrices and run full lint validation.

Purpose: Ensure the new role is tested in CI on every push, completing the CI integration requirement.
Output: Updated ci-uv.yml and ci-cd-enterprise.yml with antivirus in test matrices; 0 lint errors confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/ci-uv.yml
@.github/workflows/ci-cd-enterprise.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add antivirus to CI matrices and validate</name>
  <files>
    .github/workflows/ci-uv.yml,
    .github/workflows/ci-cd-enterprise.yml
  </files>
  <read_first>
    .github/workflows/ci-uv.yml,
    .github/workflows/ci-cd-enterprise.yml
  </read_first>
  <action>
1. Edit `.github/workflows/ci-uv.yml`:
   - Find the `test-roles` matrix `role:` list
   - Add `- antivirus` after the `- rsyslog_forwarding` entry (maintaining consistent ordering: ..., ntp_hardening, user_audit, rsyslog_forwarding, antivirus)

2. Edit `.github/workflows/ci-cd-enterprise.yml`:
   - Find the `test-molecule` matrix `role:` list
   - Add `- antivirus` after the `- rsyslog_forwarding` entry (same ordering)

3. Reinstall the collection to pick up the new role:
```bash
ansible-galaxy collection install . --force -p .ansible/collections
```

4. Run full lint validation:
```bash
yamllint roles/antivirus/
ansible-lint --profile production roles/antivirus/
```
Both must exit 0 with no errors.

5. Verify the role appears correctly in both workflows:
```bash
grep -n 'antivirus' .github/workflows/ci-uv.yml
grep -n 'antivirus' .github/workflows/ci-cd-enterprise.yml
```
  </action>
  <verify>
    <automated>yamllint roles/antivirus/ && ansible-lint --profile production roles/antivirus/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "antivirus" .github/workflows/ci-uv.yml returns "- antivirus" in the matrix
    - grep "antivirus" .github/workflows/ci-cd-enterprise.yml returns "- antivirus" in the matrix
    - yamllint roles/antivirus/ exits 0
    - ansible-lint --profile production roles/antivirus/ exits 0
  </acceptance_criteria>
  <done>
    antivirus role added to both CI matrices after rsyslog_forwarding. Full yamllint + ansible-lint pass with 0 errors. Collection reinstalled. (STD-12, STD-13, STD-14)
  </done>
</task>

</tasks>

<verification>
- `grep 'antivirus' .github/workflows/ci-uv.yml` shows matrix entry
- `grep 'antivirus' .github/workflows/ci-cd-enterprise.yml` shows matrix entry
- `yamllint roles/antivirus/` exits 0
- `ansible-lint --profile production roles/antivirus/` exits 0
</verification>

<success_criteria>
antivirus in both CI matrices. yamllint and ansible-lint pass with 0 errors on the complete role.
</success_criteria>

<output>
After completion, create `.planning/phases/04-antivirus/04-05-SUMMARY.md`
</output>
