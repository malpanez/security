---
phase: 04-antivirus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/antivirus/defaults/main.yml
  - roles/antivirus/handlers/main.yml
  - roles/antivirus/meta/main.yml
  - roles/antivirus/meta/argument_specs.yml
  - roles/antivirus/tests/test.yml
  - roles/antivirus/vars/Debian.yml
  - roles/antivirus/vars/RedHat.yml
  - roles/antivirus/vars/Suse.yml
autonomous: true
requirements: [AV-02, AV-05, AV-06, AV-07, STD-01, STD-05, STD-06, STD-07, STD-08, STD-09]

must_haves:
  truths:
    - "Role skeleton exists with all required directories"
    - "All defaults have antivirus_ prefix and sensible security defaults"
    - "OS-specific vars provide correct package names, service names, config paths per distro"
    - "Handlers restart clamd, freshclam, reload systemd with container guard"
  artifacts:
    - path: "roles/antivirus/defaults/main.yml"
      provides: "All role variables with antivirus_ prefix"
      contains: "antivirus_enabled"
    - path: "roles/antivirus/vars/RedHat.yml"
      provides: "RHEL package names and paths"
      contains: "clamd@scan"
    - path: "roles/antivirus/vars/Debian.yml"
      provides: "Debian package names and paths"
      contains: "clamav-daemon"
    - path: "roles/antivirus/vars/Suse.yml"
      provides: "SUSE package names and paths"
      contains: "clamd"
    - path: "roles/antivirus/meta/argument_specs.yml"
      provides: "All defaults documented with type/description"
      contains: "antivirus_update_db"
    - path: "roles/antivirus/meta/main.yml"
      provides: "Galaxy metadata with compliance tags"
      contains: "antivirus"
    - path: "roles/antivirus/handlers/main.yml"
      provides: "Service restart handlers"
      contains: "Restart clamd"
    - path: "roles/antivirus/tests/test.yml"
      provides: "Test playbook with FQCN"
      contains: "malpanez.security.antivirus"
  key_links:
    - from: "roles/antivirus/defaults/main.yml"
      to: "roles/antivirus/meta/argument_specs.yml"
      via: "every default var documented in argument_specs"
      pattern: "antivirus_"
---

<objective>
Scaffold the antivirus role using ansible-galaxy role init, then populate defaults, vars, meta, handlers, and tests.

Purpose: Establish the role skeleton with all variable contracts and OS-specific paths that tasks and templates will consume.
Output: Complete role scaffold with defaults/main.yml (9 vars), vars/{Debian,RedHat,Suse}.yml, meta/main.yml, meta/argument_specs.yml, handlers/main.yml, tests/test.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-antivirus/04-RESEARCH.md
@roles/ntp_hardening/defaults/main.yml
@roles/ntp_hardening/meta/argument_specs.yml
@roles/ntp_hardening/meta/main.yml
@roles/rsyslog_forwarding/handlers/main.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold role and populate defaults, vars, meta, handlers, tests</name>
  <files>
    roles/antivirus/defaults/main.yml,
    roles/antivirus/handlers/main.yml,
    roles/antivirus/meta/main.yml,
    roles/antivirus/meta/argument_specs.yml,
    roles/antivirus/tests/test.yml,
    roles/antivirus/vars/Debian.yml,
    roles/antivirus/vars/RedHat.yml,
    roles/antivirus/vars/Suse.yml
  </files>
  <read_first>
    roles/ntp_hardening/defaults/main.yml,
    roles/ntp_hardening/meta/argument_specs.yml,
    roles/ntp_hardening/meta/main.yml,
    roles/rsyslog_forwarding/handlers/main.yml,
    roles/rsyslog_forwarding/vars/RedHat.yml,
    roles/rsyslog_forwarding/vars/Debian.yml,
    roles/rsyslog_forwarding/vars/Suse.yml
  </read_first>
  <action>
1. Run `ansible-galaxy role init roles/antivirus --offline` to create the skeleton (STD-01).

2. Write `roles/antivirus/defaults/main.yml`:
```yaml
antivirus_enabled: false

antivirus_update_db: true

antivirus_scan_enabled: true

antivirus_scan_dirs:
  - /home
  - /tmp
  - /var/tmp

antivirus_scan_schedule: daily

antivirus_scan_log: /var/log/clamav/scan.log

antivirus_freshclam_checks: 12

antivirus_max_file_size: 25M

antivirus_max_scan_size: 100M

antivirus_selinux_contexts: true
```

3. Write `roles/antivirus/vars/RedHat.yml`:
```yaml
antivirus_packages:
  - clamav
  - clamd
  - clamav-update
antivirus_clamd_service: clamd@scan
antivirus_freshclam_service: clamav-freshclam
antivirus_clamd_config: /etc/clamd.d/scan.conf
antivirus_freshclam_config: /etc/freshclam.conf
antivirus_clamd_user: clamscan
antivirus_clamd_group: clamscan
antivirus_log_dir: /var/log/clamd.scan
antivirus_db_dir: /var/lib/clamav
antivirus_epel_required: true
```

4. Write `roles/antivirus/vars/Debian.yml`:
```yaml
antivirus_packages:
  - clamav
  - clamav-daemon
  - clamav-freshclam
antivirus_clamd_service: clamav-daemon
antivirus_freshclam_service: clamav-freshclam
antivirus_clamd_config: /etc/clamav/clamd.conf
antivirus_freshclam_config: /etc/clamav/freshclam.conf
antivirus_clamd_user: clamav
antivirus_clamd_group: clamav
antivirus_log_dir: /var/log/clamav
antivirus_db_dir: /var/lib/clamav
antivirus_epel_required: false
```

5. Write `roles/antivirus/vars/Suse.yml`:
```yaml
antivirus_packages:
  - clamav
antivirus_clamd_service: clamd
antivirus_freshclam_service: freshclam
antivirus_clamd_config: /etc/clamd.conf
antivirus_freshclam_config: /etc/freshclam.conf
antivirus_clamd_user: vscan
antivirus_clamd_group: vscan
antivirus_log_dir: /var/log/clamav
antivirus_db_dir: /var/lib/clamav
antivirus_epel_required: false
```

6. Write `roles/antivirus/meta/main.yml` — follow ntp_hardening pattern:
   - galaxy_info: author malpanez, description "ClamAV antivirus installation, freshclam updates, and scheduled scan timer", license MIT, min_ansible_version '2.16'
   - platforms: Debian [buster, bullseye, bookworm], Ubuntu [bionic, focal, jammy], EL versions: [all] (STD-07)
   - galaxy_tags: [antivirus, clamav, security, hardening, compliance, pci-dss, nis2, soc2] (AV-07)
   - dependencies: []

7. Write `roles/antivirus/meta/argument_specs.yml` — document ALL 10 defaults variables (STD-08):
   - antivirus_enabled: type bool, default false, description gate variable
   - antivirus_update_db: type bool, default true, description "Run freshclam to update virus database. Set false in CI to skip 200MB download."
   - antivirus_scan_enabled: type bool, default true, description "Deploy systemd scan timer"
   - antivirus_scan_dirs: type list, elements str, description "Directories to scan"
   - antivirus_scan_schedule: type str, default daily, description "systemd OnCalendar value"
   - antivirus_scan_log: type str, default /var/log/clamav/scan.log
   - antivirus_freshclam_checks: type int, default 12, description "freshclam Checks directive"
   - antivirus_max_file_size: type str, default 25M
   - antivirus_max_scan_size: type str, default 100M
   - antivirus_selinux_contexts: type bool, default true, description "Restore SELinux contexts and set antivirus_can_scan_system boolean on RHEL"

8. Write `roles/antivirus/handlers/main.yml` — three handlers, each with `changed_when: true` and container guard:
```yaml
- name: Restart clamd
  ansible.builtin.systemd:
    name: "{{ antivirus_clamd_service }}"
    state: restarted
  changed_when: true
  when: not _antivirus_in_container | bool

- name: Restart freshclam
  ansible.builtin.systemd:
    name: "{{ antivirus_freshclam_service }}"
    state: restarted
  changed_when: true
  when: not _antivirus_in_container | bool

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  changed_when: true
```

9. Write `roles/antivirus/tests/test.yml` (STD-09):
```yaml
- name: Test antivirus role
  hosts: all
  become: true
  roles:
    - role: malpanez.security.antivirus
```

10. Remove empty boilerplate files left by `ansible-galaxy role init` that are not needed: `roles/antivirus/vars/main.yml` (we use OS-family vars instead), `roles/antivirus/README.md` placeholder (keep but leave as-is for now).
  </action>
  <verify>
    <automated>yamllint roles/antivirus/defaults/main.yml roles/antivirus/vars/Debian.yml roles/antivirus/vars/RedHat.yml roles/antivirus/vars/Suse.yml roles/antivirus/meta/main.yml roles/antivirus/meta/argument_specs.yml roles/antivirus/handlers/main.yml roles/antivirus/tests/test.yml</automated>
  </verify>
  <acceptance_criteria>
    - grep "antivirus_enabled: false" roles/antivirus/defaults/main.yml
    - grep "antivirus_update_db: true" roles/antivirus/defaults/main.yml
    - grep "antivirus_scan_enabled: true" roles/antivirus/defaults/main.yml
    - grep "antivirus_selinux_contexts: true" roles/antivirus/defaults/main.yml
    - grep "clamd@scan" roles/antivirus/vars/RedHat.yml
    - grep "antivirus_epel_required: true" roles/antivirus/vars/RedHat.yml
    - grep "clamav-daemon" roles/antivirus/vars/Debian.yml
    - grep "antivirus_epel_required: false" roles/antivirus/vars/Debian.yml
    - grep "vscan" roles/antivirus/vars/Suse.yml
    - grep "versions:" roles/antivirus/meta/main.yml returns "all" not "8" or "9"
    - grep "pci-dss" roles/antivirus/meta/main.yml
    - grep "antivirus_update_db" roles/antivirus/meta/argument_specs.yml
    - grep "Restart clamd" roles/antivirus/handlers/main.yml
    - grep "Restart freshclam" roles/antivirus/handlers/main.yml
    - grep "Reload systemd" roles/antivirus/handlers/main.yml
    - grep "changed_when: true" roles/antivirus/handlers/main.yml returns 3 matches
    - grep "malpanez.security.antivirus" roles/antivirus/tests/test.yml
  </acceptance_criteria>
  <done>
    Role skeleton exists with all 10 defaults, 3 OS-family vars files with correct package/service/path mappings, Galaxy-valid meta, argument_specs covering all defaults, 3 handlers with changed_when:true and container guard, and test.yml with FQCN role reference.
  </done>
</task>

</tasks>

<verification>
- `yamllint roles/antivirus/` passes with 0 errors
- All defaults variables have `antivirus_` prefix
- vars/RedHat.yml has `clamd@scan` and `antivirus_epel_required: true`
- vars/Debian.yml has `clamav-daemon` and `antivirus_epel_required: false`
- meta/main.yml has `versions: [all]` for EL platforms
- argument_specs.yml documents all 10 defaults variables
</verification>

<success_criteria>
Role scaffold passes yamllint. All variable contracts are defined. OS-specific vars provide correct paths for RHEL, Debian, and SUSE.
</success_criteria>

<output>
After completion, create `.planning/phases/04-antivirus/04-01-SUMMARY.md`
</output>
