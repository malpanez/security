---
phase: 07-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/ntp_hardening/README.md
  - roles/user_audit/README.md
  - roles/rsyslog_forwarding/README.md
  - roles/antivirus/README.md
  - roles/usbguard/README.md
  - roles/tls_hardening/README.md
autonomous: true
requirements:
  - TECH-01

must_haves:
  truths:
    - "No role README contains the ansible-galaxy scaffold placeholder 'A brief description of the role goes here'"
    - "Each README describes what the role does, its defaults, a working example playbook, and compliance tags"
    - "tls_hardening/README.md exists (was missing entirely)"
  artifacts:
    - path: "roles/ntp_hardening/README.md"
      provides: "Role-specific documentation"
      contains: "ntp_hardening_servers"
    - path: "roles/user_audit/README.md"
      provides: "Role-specific documentation"
      contains: "user_audit_inactive_days"
    - path: "roles/rsyslog_forwarding/README.md"
      provides: "Role-specific documentation"
      contains: "rsyslog_forwarding_host"
    - path: "roles/antivirus/README.md"
      provides: "Role-specific documentation"
      contains: "antivirus_enabled"
    - path: "roles/usbguard/README.md"
      provides: "Role-specific documentation"
      contains: "usbguard_enabled"
    - path: "roles/tls_hardening/README.md"
      provides: "Role-specific documentation"
      contains: "tls_hardening_enabled"
  key_links:
    - from: "README.md variables table"
      to: "defaults/main.yml"
      via: "variable names must match exactly"
      pattern: "ntp_hardening_|user_audit_|rsyslog_forwarding_|antivirus_|usbguard_|tls_hardening_"
---

<objective>
Replace ansible-galaxy scaffold placeholder READMEs with real documentation for 5 roles, and create the missing tls_hardening README.

Purpose: Pre-Galaxy-publish gate — Ansible Galaxy displays READMEs on role pages; placeholder text disqualifies publication.
Output: 6 README.md files with role description, requirements, defaults table, example playbook, compliance tags, license.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- README structure to use for all 6 roles. Follow this exact section order. -->
<!--
# Role Name

Brief one-sentence description.

## Requirements

Ansible version, collection dependencies.

## Role Variables

| Variable | Default | Description |
|----------|---------|-------------|
| ...      | ...     | ...         |

## Example Playbook

```yaml
- hosts: all
  roles:
    - role: malpanez.security.<role_name>
      vars:
        <key_var>: <value>
```

## Compliance

| Framework | Controls |
|-----------|----------|
| ...       | ...      |

## License

<license from meta/main.yml>
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write READMEs for ntp_hardening, user_audit, rsyslog_forwarding</name>
  <files>
    roles/ntp_hardening/README.md
    roles/user_audit/README.md
    roles/rsyslog_forwarding/README.md
  </files>
  <action>
Overwrite each file with role-specific content. Use meta/main.yml for description, license, and compliance tags. Use meta/argument_specs.yml for the variables table (all options, their type, default, and description condensed to one line). Use defaults/main.yml to confirm variable names and defaults match exactly.

ntp_hardening README:
- Description: NTP/chrony hardening. Installs chrony, disables competing time daemons, deploys hardened chrony.conf. Supports review mode (audit-only) and enforce mode.
- Requirements: Ansible >= 2.16, malpanez.security collection
- Variables table: all 11 vars from argument_specs — ntp_hardening_enabled, ntp_hardening_servers, ntp_hardening_makestep, ntp_hardening_rtcsync, ntp_hardening_maxdistance, ntp_hardening_minsources, ntp_hardening_allow, ntp_hardening_deny_all, ntp_hardening_leapsectz, ntp_hardening_logdir, ntp_hardening_logchange
- Example: role with ntp_hardening_enabled: true, security_mode: enforce, two ntp servers
- Compliance: PCI-DSS 10.6.1, NIS2 Art.21, SOC2 CC6.1
- License: GPL-2.0-or-later

user_audit README:
- Description: Local user account audit and lockout enforcement. Reviews password expiry, inactive accounts, UID 0 duplicates, service account shells. Enforces policy via chage and usermod.
- Variables table: all 9 vars — user_audit_enabled, user_audit_inactive_days, user_audit_max_password_age, user_audit_min_password_age, user_audit_warn_days, user_audit_lock_inactive, user_audit_set_password_expiry, user_audit_fix_service_shells, user_audit_skip_users
- Example: role with user_audit_enabled: true, security_mode: enforce
- Compliance: SOC2 CC6.2/CC6.3, HIPAA, PCI-DSS 8.1.4/8.3.6, NIS2
- License: GPL-2.0-or-later

rsyslog_forwarding README:
- Description: Centralised log forwarding via rsyslog drop-in. Deploys /etc/rsyslog.d/99-forwarding.conf with RainerScript action() syntax, optional GnuTLS TLS, and disk-assisted queue. Never modifies /etc/rsyslog.conf.
- Variables table: all 17 vars. Group them into subsections: Core, Queue, TLS, Logrotate.
- Example: role with rsyslog_forwarding_enabled: true, security_mode: enforce, rsyslog_forwarding_host: "siem.corp.example.com"
- Compliance: NIS2 Art.21, SOC2 CC7.2, PCI-DSS 10.5
- License: GPL-2.0-or-later
  </action>
  <verify>
    <automated>grep -rL "A brief description of the role goes here" roles/ntp_hardening/README.md roles/user_audit/README.md roles/rsyslog_forwarding/README.md | wc -l | grep -q '^3$' && echo PASS || echo FAIL</automated>
  </verify>
  <done>All 3 READMEs exist with role-specific content, no scaffold placeholder text, variables table matches defaults/main.yml exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Write READMEs for antivirus, usbguard, and create tls_hardening README</name>
  <files>
    roles/antivirus/README.md
    roles/usbguard/README.md
    roles/tls_hardening/README.md
  </files>
  <action>
antivirus README:
- Description: ClamAV antivirus installation, freshclam updates, and scheduled scan timer. Supports EPEL (RHEL) and container-aware skipping. Review mode audits ClamAV state; enforce mode installs and configures.
- Variables table: all 10 vars — antivirus_enabled, antivirus_update_db (note: set false in CI to skip 200MB download), antivirus_scan_enabled, antivirus_scan_dirs, antivirus_scan_schedule, antivirus_scan_log, antivirus_freshclam_checks, antivirus_max_file_size, antivirus_max_scan_size, antivirus_selinux_contexts
- Also document Phase 06.1 changes: the clamav-scan.service runs as the ClamAV daemon user (not root) per AUDIT-MED-02 remediation
- Example: role with antivirus_enabled: true, security_mode: enforce, antivirus_update_db: false (CI-safe)
- Compliance: PCI-DSS 5.2, NIS2 Art.21(2)(e), SOC2 CC6.8
- License: MIT

usbguard README:
- Description: USB device control with block-by-default policy. Generates initial device whitelist from attached hardware, deploys usbguard-daemon.conf, starts and enables USBGuard. Gracefully skips enforcement in container environments (no USB bus).
- Variables table: all 9 vars — usbguard_enabled, usbguard_implicit_policy (choices: allow/block/reject), usbguard_allow_existing_devices, usbguard_present_device_policy, usbguard_present_controller_policy, usbguard_restore_controller_state, usbguard_audit_backend (choices: LinuxAudit/FileAudit), usbguard_audit_file_path, usbguard_extra_rules
- Include a NOTE: first run with usbguard_allow_existing_devices: true captures currently-attached devices. After that, new devices are blocked by default. Review attached devices before running in enforce mode.
- Example: role with usbguard_enabled: true, security_mode: enforce, usbguard_allow_existing_devices: true
- Compliance: PCI-DSS 12.3.4, HIPAA 164.310(d)(1), NIS2 Art.21, SOC2 CC6.7
- License: GPL-2.0-or-later

tls_hardening README (create from scratch — file does not exist):
- Description: System-wide TLS/crypto policy hardening. Uses update-crypto-policies on RHEL 8/9 and openssl.cnf lineinfile on Debian/Ubuntu. Supports review mode (reports current posture) and enforce mode (applies target policy). NOTE: policy changes apply to new connections; callers must reload daemons that hold open TLS sockets.
- Variables table: all 4 vars — tls_hardening_enabled, tls_hardening_rhel_policy (choices: DEFAULT/DEFAULT:NO-SHA1/FUTURE/FIPS; RHEL 8/9 only), tls_hardening_min_protocol (choices: TLSv1.2/TLSv1.3; Debian/Ubuntu only), tls_hardening_openssl_cipher_string (Debian/Ubuntu only)
- Example playbook showing RHEL usage and separate Debian usage via host_vars
- Compliance: PCI-DSS 4.2.1, HIPAA, NIS2 Art.21, SOC2 CC6.7
- License: MIT
  </action>
  <verify>
    <automated>test -f roles/tls_hardening/README.md && grep -rL "A brief description of the role goes here" roles/antivirus/README.md roles/usbguard/README.md roles/tls_hardening/README.md | wc -l | grep -q '^3$' && echo PASS || echo FAIL</automated>
  </verify>
  <done>All 3 files written with role-specific content. tls_hardening/README.md exists (was missing). No scaffold placeholder text in any file.</done>
</task>

</tasks>

<verification>
Run after both tasks:

```bash
for role in ntp_hardening user_audit rsyslog_forwarding antivirus usbguard tls_hardening; do
  if grep -q "A brief description of the role goes here" roles/$role/README.md 2>/dev/null; then
    echo "FAIL: $role still has placeholder"
  elif [ ! -f "roles/$role/README.md" ]; then
    echo "FAIL: $role README missing"
  else
    echo "PASS: $role"
  fi
done
```

All 6 lines must output PASS.
</verification>

<success_criteria>
- 6 READMEs written with role-specific content
- No file contains "A brief description of the role goes here"
- roles/tls_hardening/README.md exists (was missing)
- Variable names in tables match defaults/main.yml exactly (no typos)
- Each README has: description, requirements, variables table, example playbook, compliance section, license
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt/07-tech-debt-01-SUMMARY.md`
</output>
