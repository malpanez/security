---
phase: 05-tls-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - roles/tls_hardening/defaults/main.yml
  - roles/tls_hardening/vars/RedHat.yml
  - roles/tls_hardening/vars/Debian.yml
  - roles/tls_hardening/vars/Suse.yml
  - roles/tls_hardening/meta/main.yml
  - roles/tls_hardening/meta/argument_specs.yml
  - roles/tls_hardening/handlers/main.yml
  - roles/tls_hardening/tests/test.yml
autonomous: true
requirements:
  - STD-01
  - STD-05
  - STD-06
  - STD-07
  - STD-08
  - STD-09
  - TLS-05
  - TLS-06

must_haves:
  truths:
    - "Role directory exists at roles/tls_hardening/ with standard Ansible role structure"
    - "defaults/main.yml contains all 4 configurable variables with correct types and defaults"
    - "Three OS-family var files provide OS-specific openssl.cnf paths"
    - "meta/main.yml passes Galaxy schema validation"
    - "argument_specs.yml documents all defaults variables"
  artifacts:
    - path: "roles/tls_hardening/defaults/main.yml"
      provides: "Role defaults with tls_hardening_enabled, rhel_policy, min_protocol, cipher_string"
      contains: "tls_hardening_enabled"
    - path: "roles/tls_hardening/vars/RedHat.yml"
      provides: "RHEL-specific openssl.cnf path"
      contains: "/etc/pki/tls/openssl.cnf"
    - path: "roles/tls_hardening/vars/Debian.yml"
      provides: "Debian-specific openssl.cnf path"
      contains: "/etc/ssl/openssl.cnf"
    - path: "roles/tls_hardening/vars/Suse.yml"
      provides: "SUSE-specific openssl.cnf path"
      contains: "/etc/ssl/openssl.cnf"
    - path: "roles/tls_hardening/meta/main.yml"
      provides: "Galaxy metadata with compliance tags"
      contains: "tls_hardening"
    - path: "roles/tls_hardening/meta/argument_specs.yml"
      provides: "Variable documentation"
      contains: "tls_hardening_min_protocol"
    - path: "roles/tls_hardening/handlers/main.yml"
      provides: "Empty handlers file (no service restarts per D-21)"
    - path: "roles/tls_hardening/tests/test.yml"
      provides: "FQCN test playbook"
      contains: "malpanez.security.tls_hardening"
  key_links:
    - from: "roles/tls_hardening/defaults/main.yml"
      to: "roles/tls_hardening/meta/argument_specs.yml"
      via: "Every default variable documented in argument_specs"
      pattern: "tls_hardening_"
---

<objective>
Scaffold the tls_hardening role via ansible-galaxy role init, then populate defaults, OS-family vars, meta, handlers, and tests with the correct structure and values.

Purpose: Establish the role skeleton that plans 02-05 build upon. All configurable variables, OS paths, Galaxy metadata, and compliance tags are defined here.
Output: Complete role scaffold at roles/tls_hardening/ passing yamllint and ansible-lint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tls-hardening/05-CONTEXT.md
@.planning/phases/05-tls-hardening/05-RESEARCH.md

@roles/antivirus/defaults/main.yml
@roles/antivirus/meta/main.yml
@roles/antivirus/meta/argument_specs.yml
@roles/antivirus/vars/RedHat.yml
@roles/antivirus/vars/Debian.yml
@roles/antivirus/handlers/main.yml
@roles/antivirus/tests/test.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold tls_hardening role and populate all static files</name>
  <files>
    roles/tls_hardening/defaults/main.yml,
    roles/tls_hardening/vars/RedHat.yml,
    roles/tls_hardening/vars/Debian.yml,
    roles/tls_hardening/vars/Suse.yml,
    roles/tls_hardening/meta/main.yml,
    roles/tls_hardening/meta/argument_specs.yml,
    roles/tls_hardening/handlers/main.yml,
    roles/tls_hardening/tests/test.yml
  </files>
  <read_first>
    roles/antivirus/defaults/main.yml,
    roles/antivirus/meta/main.yml,
    roles/antivirus/meta/argument_specs.yml,
    roles/antivirus/vars/RedHat.yml,
    roles/antivirus/vars/Debian.yml,
    roles/antivirus/vars/Suse.yml,
    roles/antivirus/handlers/main.yml,
    roles/antivirus/tests/test.yml,
    .planning/phases/05-tls-hardening/05-RESEARCH.md
  </read_first>
  <action>
1. Run `ansible-galaxy role init roles/tls_hardening --force` to create the skeleton (per STD-01).

2. **defaults/main.yml** — exactly 4 variables (per D-01, D-02, D-10, D-11, D-12, D-19, D-20):
   ```yaml
   ---
   tls_hardening_enabled: false
   tls_hardening_rhel_policy: "DEFAULT:NO-SHA1"
   tls_hardening_min_protocol: "TLSv1.2"
   tls_hardening_openssl_cipher_string: "HIGH:!aNULL:!MD5:!RC4:!3DES"
   ```

3. **vars/RedHat.yml** (per D-08, RESEARCH vars/ design):
   ```yaml
   ---
   tls_hardening_openssl_cnf: /etc/pki/tls/openssl.cnf
   ```

4. **vars/Debian.yml**:
   ```yaml
   ---
   tls_hardening_openssl_cnf: /etc/ssl/openssl.cnf
   ```

5. **vars/Suse.yml**:
   ```yaml
   ---
   tls_hardening_openssl_cnf: /etc/ssl/openssl.cnf
   ```

6. **meta/main.yml** — follow antivirus pattern exactly. Platforms: Debian [buster,bullseye,bookworm], Ubuntu [bionic,focal,jammy], EL versions:[all], Suse versions:[all]. galaxy_tags include: tls, hardening, security, compliance, pcidss, hipaa, nis2, soc2, openssl, cryptopolicy. min_ansible_version: '2.16'. Role name: tls_hardening. No dependencies.

7. **meta/argument_specs.yml** — document all 4 defaults variables with type, default, description, and choices where applicable:
   - tls_hardening_enabled: type bool, default false, description "Enable TLS hardening enforcement"
   - tls_hardening_rhel_policy: type str, default "DEFAULT:NO-SHA1", description "RHEL 8/9 crypto-policies policy string"
   - tls_hardening_min_protocol: type str, default "TLSv1.2", description "Minimum TLS protocol version for openssl.cnf", choices [TLSv1.2, TLSv1.3]
   - tls_hardening_openssl_cipher_string: type str, default "HIGH:!aNULL:!MD5:!RC4:!3DES", description "OpenSSL CipherString for openssl.cnf"

8. **handlers/main.yml** — empty with comment explaining design decision D-21:
   ```yaml
   ---
   # tls_hardening: no service restarts — policy changes apply to new connections.
   # Callers must reload their own daemons after applying tls_hardening.
   ```

9. **tests/test.yml** — FQCN pattern (per STD-09):
   ```yaml
   ---
   - name: Test tls_hardening role
     hosts: all
     become: true
     gather_facts: true
     roles:
       - role: malpanez.security.tls_hardening
   ```

10. Delete galaxy-init boilerplate: remove roles/tls_hardening/README.md (will be written later or in a separate task), remove empty tasks/main.yml (plan 02 creates it).

11. Reinstall collection: `ansible-galaxy collection install . --force -p .ansible/collections` (per STD-14).

12. Run `yamllint roles/tls_hardening/` and `ansible-lint --profile production roles/tls_hardening/` — fix any issues until 0 errors.
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/malpanez/security && yamllint roles/tls_hardening/ && ansible-lint --profile production roles/tls_hardening/</automated>
  </verify>
  <acceptance_criteria>
    - `roles/tls_hardening/defaults/main.yml` contains exactly: `tls_hardening_enabled: false`, `tls_hardening_rhel_policy: "DEFAULT:NO-SHA1"`, `tls_hardening_min_protocol: "TLSv1.2"`, `tls_hardening_openssl_cipher_string: "HIGH:!aNULL:!MD5:!RC4:!3DES"`
    - `roles/tls_hardening/vars/RedHat.yml` contains `/etc/pki/tls/openssl.cnf`
    - `roles/tls_hardening/vars/Debian.yml` contains `/etc/ssl/openssl.cnf`
    - `roles/tls_hardening/vars/Suse.yml` contains `/etc/ssl/openssl.cnf`
    - `roles/tls_hardening/meta/main.yml` has `versions: [all]` for EL platform
    - `roles/tls_hardening/meta/argument_specs.yml` documents all 4 variables
    - `roles/tls_hardening/tests/test.yml` contains `malpanez.security.tls_hardening`
    - `roles/tls_hardening/handlers/main.yml` exists and contains no handler definitions (comment only)
    - `yamllint roles/tls_hardening/` exits 0
    - `ansible-lint --profile production roles/tls_hardening/` exits 0
  </acceptance_criteria>
  <done>Role scaffold complete at roles/tls_hardening/ with all static files populated, yamllint and ansible-lint pass with 0 errors, collection reinstalled.</done>
</task>

</tasks>

<verification>
- `ls roles/tls_hardening/{defaults,vars,meta,handlers,tests}/` shows all expected files
- `grep -c 'tls_hardening_' roles/tls_hardening/defaults/main.yml` returns 4
- `grep 'malpanez.security.tls_hardening' roles/tls_hardening/tests/test.yml` matches
- `yamllint roles/tls_hardening/ && ansible-lint --profile production roles/tls_hardening/` exits 0
</verification>

<success_criteria>
Role skeleton exists with correct defaults, OS vars, Galaxy-valid meta, argument specs, empty handlers, and FQCN tests. Lint passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tls-hardening/05-01-SUMMARY.md`
</output>
