---
phase: 05-tls-hardening
plan: "05"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - .github/workflows/ci-uv.yml
  - .github/workflows/ci-cd-enterprise.yml
autonomous: true
requirements:
  - STD-13
  - STD-14

must_haves:
  truths:
    - "tls_hardening appears in ci-uv.yml test-roles matrix after antivirus"
    - "tls_hardening appears in ci-cd-enterprise.yml test-molecule matrix after antivirus"
    - "Collection is reinstalled after adding the role"
    - "yamllint and ansible-lint pass with 0 errors on the entire role"
  artifacts:
    - path: ".github/workflows/ci-uv.yml"
      provides: "CI matrix with tls_hardening in test-roles"
      contains: "tls_hardening"
    - path: ".github/workflows/ci-cd-enterprise.yml"
      provides: "Enterprise CI matrix with tls_hardening in test-molecule"
      contains: "tls_hardening"
  key_links:
    - from: ".github/workflows/ci-uv.yml"
      to: "roles/tls_hardening/"
      via: "test-roles matrix entry"
      pattern: "tls_hardening"
    - from: ".github/workflows/ci-cd-enterprise.yml"
      to: "roles/tls_hardening/"
      via: "test-molecule matrix entry"
      pattern: "tls_hardening"
---

<objective>
Add tls_hardening to both CI workflow matrices and run final lint validation on the complete role.

Purpose: Ensure the role is tested in CI on every push, consistent with all prior roles.
Output: Updated CI workflow files with tls_hardening in both matrices. Full lint pass on the role.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tls-hardening/05-CONTEXT.md
@.planning/phases/05-tls-hardening/05-01-SUMMARY.md

@.github/workflows/ci-uv.yml
@.github/workflows/ci-cd-enterprise.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tls_hardening to CI matrices and run final validation</name>
  <files>
    .github/workflows/ci-uv.yml,
    .github/workflows/ci-cd-enterprise.yml
  </files>
  <read_first>
    .github/workflows/ci-uv.yml,
    .github/workflows/ci-cd-enterprise.yml
  </read_first>
  <action>
1. **ci-uv.yml** — Find the test-roles matrix (around line 137). Add `- tls_hardening` on a new line immediately after `- antivirus`. The ordering follows the ROADMAP phase sequence: ..., user_audit, rsyslog_forwarding, antivirus, tls_hardening.

2. **ci-cd-enterprise.yml** — Find the test-molecule matrix (around line 340). Add `- tls_hardening` on a new line immediately after `- antivirus`. Same ordering as ci-uv.yml.

3. Reinstall collection: `ansible-galaxy collection install . --force -p .ansible/collections` (per STD-14).

4. Run full lint on the complete role:
   ```bash
   yamllint roles/tls_hardening/
   ansible-lint --profile production roles/tls_hardening/
   ```
   Fix any issues until 0 errors.

5. Verify the CI files themselves are valid YAML: `yamllint .github/workflows/ci-uv.yml .github/workflows/ci-cd-enterprise.yml`.
  </action>
  <verify>
    <automated>cd /home/malpanez/repos/malpanez/security && grep 'tls_hardening' .github/workflows/ci-uv.yml .github/workflows/ci-cd-enterprise.yml && yamllint roles/tls_hardening/ && ansible-lint --profile production roles/tls_hardening/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tls_hardening' .github/workflows/ci-uv.yml` returns at least 1
    - `grep -c 'tls_hardening' .github/workflows/ci-cd-enterprise.yml` returns at least 1
    - `grep -A1 'antivirus' .github/workflows/ci-uv.yml | grep 'tls_hardening'` matches (tls_hardening is right after antivirus)
    - `grep -A1 'antivirus' .github/workflows/ci-cd-enterprise.yml | grep 'tls_hardening'` matches
    - `yamllint roles/tls_hardening/` exits 0
    - `ansible-lint --profile production roles/tls_hardening/` exits 0
    - `ls .ansible/collections/ansible_collections/malpanez/security/roles/tls_hardening/` shows role files (collection reinstalled)
  </acceptance_criteria>
  <done>tls_hardening added to both CI matrices after antivirus. Collection reinstalled. Full lint passes with 0 errors.</done>
</task>

</tasks>

<verification>
- `grep 'tls_hardening' .github/workflows/ci-uv.yml .github/workflows/ci-cd-enterprise.yml` shows both files
- `yamllint roles/tls_hardening/ && ansible-lint --profile production roles/tls_hardening/` exits 0
</verification>

<success_criteria>
Role is fully integrated into CI. Both matrices include tls_hardening. Full lint validation passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tls-hardening/05-05-SUMMARY.md`
</output>
