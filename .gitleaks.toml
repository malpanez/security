title = "malpanez/security gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Ignore common non-secret placeholders"
regexes = [
  "example",
  "EXAMPLE",
  "placeholder",
]
# NOTE: "CHANGEME" was intentionally removed from this allowlist.
# It should never appear in deployed configuration — use variables instead.
